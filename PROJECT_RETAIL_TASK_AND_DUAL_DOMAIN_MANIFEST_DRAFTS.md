# Retail task and dual-domain snapshot/split manifest drafts

**Package state:** `RETAIL_TASK_AND_DUAL_DOMAIN_MANIFEST_DRAFTS_SYNTHETICALLY_VALIDATED`  
**Project state:** `DRAFT_NOT_EXECUTABLE`  
**Reported date:** `2026-08-31`  
**Real source, documentation, license, governance, or data contacted:** no  
**Real snapshot, split, escrow, admission, or science performed:** no

## 1. Exact authority and internal renewed-scope review

The exact normalized visible overnight authority text is:

> Okay, sounds good. What I want you to do is to set aside a significant portion of work to do such that you are busy for around 8 hours, because I am going to sleep, and dont want my absence to make you idle.

It is 207 UTF-8 bytes and its SHA-256 is
`44ed1336dd467043e3daebe7ad85093c5ab954921a895483153c98cb6d32bb9a`.
Only that visible text is bound. Raw transport bytes, trailing framing, the
conversation envelope, account identity, timestamps, and cryptographic user
authentication are not bound.

This package is an additional layer beyond the already stopped PhysioNet task,
PhysioNet split, and Retail split packages. An explicit internal renewed-scope
review was therefore performed. The narrow determination is that the overnight
instruction permits this bounded local-only layer solely because it completes
the named Solo Block 5 draft milestone:

> Complete Retail task/schema draft and both snapshot/split manifest drafts.

That determination is agent adjudication, not a claim that the user selected
the filenames, schemas, task representation, or algorithms. It grants no
network or connector use, source or documentation contact, license or
governance inquiry, authentication, data access or opening, entropy, runtime
approval, scientific execution, training, result inspection, submission,
claim promotion, or tracker edit. This package performs none of those actions.

The additive five-file package is:

- `src/heterodiff/data/dual_domain_snapshot_split_manifest_drafts.py`;
- `PROJECT_RETAIL_TASK_AND_DUAL_DOMAIN_MANIFEST_DRAFTS.md`;
- `research/fixtures/manuscript_v3_retail_task_and_dual_domain_manifest_drafts_v1.json`;
- `research/diagnostics/manuscript_v3_retail_task_and_dual_domain_manifest_drafts_v1.py`; and
- `tests/unit/test_manuscript_v3_retail_task_and_dual_domain_manifest_drafts_v1.py`.

After exact independent review, the package may support only
`RETAIL_TASK_SCHEMA_AND_DUAL_DOMAIN_SNAPSHOT_SPLIT_MANIFEST_DRAFTS_VALIDATED`.
That is a project-control draft predicate with zero scientific effect.

## 2. Immutable predecessor boundary

Before executing the new pure source from a stable in-memory byte buffer, the
validator hard-pins the exact bytes of:

- the human and machine execution preregistration;
- the human and machine V2 pre-execution closure;
- all four prospective no-acquisition-seal files;
- all four stopped PhysioNet task-route files;
- all four stopped PhysioNet patient-disjoint split-design files; and
- all four stopped Retail customer-disjoint temporal split-design files.

The predecessor roster is one-way. Mutable timetable and evidence-ledger files
are neither inputs nor reverse bindings. The package neither modifies nor
reinterprets any predecessor. It consumes the exact existing split contracts:
the PhysioNet patient hash/Hamilton 70/15/15 rule and the Retail
customer-disjoint temporal/Hamilton 70/15/15 rule. The candidate proportions
remain unpowered and F061 remains open.

The validator never pathname-imports either predecessor validator. It first
stable-reads and hard-pins all predecessor bytes and the new source, then
compiles the verified new source and both verified parent splitter buffers in
memory. No pathname loader or bytecode cache can substitute different bytes.
It compares the new PhysioNet and Retail assignments or exact refusal codes
against `split_physionet_manifest` and `split_retail_rows` over 396
constructive, permutation, edge, malformed, infeasible, and small exhaustive
fixtures. This is a strong qualified-domain differential check, not a claim of
universal program equivalence.

## 3. Retail task/schema route draft

The route is an agent-selected candidate, not a verified external-source fact.
Its unit is a future canonical customer group. Candidate `Y` is the complete
admitted customer transaction line-item event configuration, candidate `z` is
a future frozen context schema, and candidate `A` is a future
application-justified partial-observation kernel. These labels organize a
future decision; they do not establish the source fields or meanings needed to
populate it.

No current claim is made about archive version, source columns, customer-key
semantics, timestamps or timezone, cancellations, country, line-item
simultaneity, units, marks, horizon, cap, segmentation, exclusions, license,
governance, observation, common support, or admission. Every F038--F057 value
is instead registered as a null `OPEN_FUTURE_TYPED_RECEIPT_REQUIRED` slot with
an exact expected receipt type. F059--F061 are likewise future typed receipts.
Missing, ambiguous, inconsistent, or unbound receipts make the Retail domain
`NO_GO`; there is no inferred default.

## 4. Content-addressed snapshot schema

PhysioNet and Retail have separate closed schema names and digest domains but
the same strict field roster. A snapshot manifest contains:

- exact domain and slot identifiers;
- one closed receipt state: synthetic qualification, or future population
  after separate authority;
- one matching verification state: synthetic receipts are not external
  evidence, while future receipts are structural-only and not independently
  verified by this package;
- the fixed custody class `INTERNAL_RESTRICTED_NOT_PUBLICATION_SAFE`;
- a nonempty printable-ASCII snapshot version;
- SHA-256 receipts for source version, raw snapshot, archive inventory, source
  schema, license/access, and governance approval;
- a strict positive raw byte count;
- the exact domain-specific normalization-contract identifier;
- the exact content digest and positive record count of the normalized
  projection;
- the exact split-schema, stopped split-predicate, and stopped split-package
  machine-byte digest crosslinks;
- zero post-snapshot exclusions and zero retry/resplit/top-up count; and
- a domain-separated SHA-256 commitment over every preceding field.

All digests are exactly 64 lowercase hexadecimal characters. All integer
fields reject Booleans. Extra or missing keys fail closed. The snapshot is
created before assignment, so it crosslinks the exact split *contract* without
creating a digest cycle. The later split manifest crosslinks the exact snapshot
content digest.

No real snapshot manifest is present. The package's constructor can create
only explicitly labeled, deterministic synthetic qualification receipts.
Those receipts are not external evidence and cannot fill F019/F020 or
F038/F039.

## 5. Content-addressed split schema

Each split manifest is an internal private record with a closed field roster:

- exact domain, slot, receipt state, custody class, and schema;
- the exact snapshot-manifest digest and normalized-projection digest;
- the exact stopped splitter predicate, algorithm identifier, and package
  machine-byte digest;
- the exact integer allocation `70/15/15` over denominator `100`;
- an explicit allocation-power-review state and receipt slot: synthetic
  manifests are `NOT_POWER_APPROVED` with a null receipt; a future structural
  instance must bind a receipt hash but that receipt remains independently
  unverified here and does not close F061;
- the complete normalized private projection;
- the complete canonical assignment output of the stopped splitter contract;
- exact zero exclusion, retry, resplit, and top-up counters; and
- a domain-separated SHA-256 commitment over every preceding field.

The normalized projection is deliberately present so the assignment can be
recomputed without trusting a detached digest. For PhysioNet the package
recomputes minimal-decimal patient IDs, patient-order SHA-256 with canonical
collision tie break, Hamilton counts, complete patient/record assignments,
and the stopped assignment digest. For Retail it recomputes opaque customer
groups, complete closed intervals, lexicographically first feasible temporal
gap pair, Hamilton counts, complete customer/row assignments, and the stopped
assignment digest.

The split manifest must match the snapshot projection digest and count exactly.
Every normalized row is preserved. Any mismatch, missing or extra field,
wrong strict type, assignment change, digest change, group split, boundary
failure, exclusion, retry, resplit, or top-up rejects the whole manifest.
There is no alternate seed, allocation, boundary, patient/customer migration,
quarantine, favorable selection, or repair.

## 6. Exact inherited normalized projections

The PhysioNet projection consumes the stopped patient-disjoint contract
unchanged: exact dictionaries containing only strict integer
`record_ordinal` and a minimal positive ASCII-decimal `patient_id`, with
ordinals exactly `0..R-1` and at least five patient groups. It does not claim
that `RecordID` is a currently verified external-source field.

The Retail projection consumes the stopped temporal-split contract unchanged:
exact dictionaries containing only strict integer `row_ordinal`, nonempty
lowercase even-length `customer_key_hex`, and strict signed-64-bit
`timestamp_utc_microseconds`, with ordinals exactly `0..R-1` and at least five
customer groups. It does not claim any current source column, identifier,
timezone, or timestamp fact.

The two projections are internal custody material. A failure to represent
every row of a future immutable snapshot is domain no-go, not permission to
exclude, retry, resplit, top up, or alter the frozen rule.

## 7. Publication and anonymity boundary

This package is internal research custody evidence. A future real split
manifest would contain normalized patient/customer identifiers and Retail
timestamps and is not safe for public or anonymous release. The visible
authority text, internal paths, hashes, receipts, and private projection are
also excluded from any automatic publication derivative.

A separately reviewed derivative must remove or aggregate private carriers,
bind only the minimum reproducibility receipts that may lawfully be disclosed,
undergo a fresh anonymity and license/governance review, and preserve the
scientific nonclaims. No public derivative is created here. The package
contains no credentials, tokens, cookies, secrets, real identifiers, source
responses, raw data, test data, protected outcomes, or scientific results.

## 8. Exact nonclosure

This package closes zero preregistration fields, zero blockers, zero formal
tests, and zero results. F019--F061 remain open; in particular every F038--F061
value remains null and open. B02, B03, and B09 remain open, as do B01--B12.
Formal Tests 28, 29, and 30 remain open, and all four result slots remain
unfilled. Existing component qualifications are not promoted into formal-test
closure.

No mutable project total is asserted or bound by this package. Its exact delta
is zero fields, zero blockers, zero formal tests, and zero results. No real
task feasibility, snapshot, split, power, governance, license, admission,
escrow, data, runtime, or science state changes.

## 9. Synthetic qualification and stop rule

The hostile suite uses only deterministic in-memory synthetic rows and pytest
temporary replicas. It covers both constructive domains, exact stopped
assignment equivalence, canonical snapshot and split digests, crosslinks,
strict types, closed key rosters, projection preservation, input permutation
invariance, malformed IDs/keys/timestamps/ordinals, digest changes, wrong
package receipts, snapshot/split mismatch, nonzero exclusion/retry/resplit/
top-up counters, Retail infeasible temporal intervals, predecessor mutation,
source mutation before execution, and ignored alternate bytecode.
The suite also rejects recursive string/integer/container subclasses,
Boolean-as-integer substitutions, nested assignment impostors, and omitted or
flipped allocation-power fields.

Ordinary interpreter and pytest processes are software qualification only.
Final commands disable Python bytecode and pytest's cache provider. The new
source exposes no file, network, subprocess, entropy, data, runtime, training,
or scientific action. The validator is read-only; hostile writes are confined
to temporary replicas.

The package stops after canonical bytes, exact hashes/sizes, and local tests
are reported. It claims no independent audit. Tracker integration is forbidden
until a separate reviewer returns an exact GO for the stopped bytes.
