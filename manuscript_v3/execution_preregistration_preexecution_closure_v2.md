# Manuscript-v3 pre-execution closure V2

**Closure ID:** `EXECUTION-PREREGISTRATION-PREEXECUTION-CLOSURE-V2`  
**Registration mode:** `ADDITIVE_SUCCESSOR_SIDECAR`  
**Global state:** `DRAFT_NOT_EXECUTABLE`  
**Current milestone:** `R1_RANK_BINDER_QUALIFIED_ZERO_EXECUTION`  
**Confirmatory execution authorized:** no  
**Production execution authorized:** no  
**Scientific-result or claim effect:** none

## 1. What this milestone closes—and what it does not

This is the largest honest additive closure supported by the present bytes.
It projects two finite-A1 fields that were fixed before D1, records D1 as
prior observed development knowledge, and statically qualifies the existing
rank-prefix source boundary. It does not edit or supersede the historical
claim ledger, execution preregistration, C17 target, V2 freeze, D1 freeze, D1
result, or E-A1-D1 registration.

`R1_RANK_BINDER_QUALIFIED_ZERO_EXECUTION` means only that a read-only,
closed-world qualification module reopened the exact bound files, checked the
current production-order and rank-stress source shapes, confirmed their false
execution gates, and confirmed that no formal runtime manifest or production
root exists. It is a static schema-and-custody prefix qualification. It is not
a completed runner binder, production plan, runtime approval, phase
consumption, coordinate permit, rank result, R1 result, or execution receipt.

No production-plan initialization, phase consumption, permit issuance, rank
computation, worker launch, model training, diagnostic rerun, or scientific
computation was performed to create this closure. The qualification record is
not reusable as an execution permit, and D1 is not admissible as production or
confirmatory evidence.

## 2. Immutable predecessor custody

The closure binds the historical claim ledger and execution preregistration:

| Bound predecessor | Raw SHA-256 |
|---|---|
| `manuscript_v3/claim_ledger.md` | `793f7fbda938f66d771af3dc480d13dc784862a439ee65452b79c776d78e8245` |
| `manuscript_v3/execution_preregistration.md` | `a68215e77fe7d20dd0738e9f758f6037c2cd69304c98e92670ded3af3e00b64e` |
| `research/fixtures/manuscript_v3_execution_preregistration_v1.json` | `edd572fc8d8c1b72ad3bc947c4427b79095d45b4d9f44371c5806066d71b0706` |
| `tests/unit/test_manuscript_v3_scientific_route.py` | `a76b2b7390999d2f43c1a7406f83f8347951d43b9762f3960410de3b188b01ae` |
| `research/fixtures/cp76_manuscript_v3_submission_readiness_manifest_v1.json` | `b9ce9744b64212bf0e762d3342c9a221438c2676ebd9d69db2f50cbbebf9ac06` |
| `tests/unit/test_manuscript_v3_submission_readiness.py` | `410a20e9444e5005481c2bb7c8acef0135061a86ce5bf3ad546fe3fffe83dcbc` |

It also binds the pre-D1 A1 specification
`475f4f450cb5703e6773c0d0ff242db995a16408acce5989401fa0674326e67c`,
the current C17 theorem target
`d11dc3a98d19a52e7ab653aca1e06598490ad098a450b526870508b4499b9d8d`,
and the finite-A1 C17 component contract
`063a9acabd79a3c329aa721aded5c4ec8804749aaccde3d8e2096c41d5ce78c8`.

The V2 freeze triplet is bound at raw SHA-256 values
`6639e0f15592558f03bae98fd7d75a56ec64564132f9631832c360a2be60f953`,
`b0b892db1041267defe664f59d57801e723f0115b8ac5ae9fc8656c3708cd8fc`,
and
`fb5f6a4571d6fea7f8d7b7254648770e9d459d10a481bc3742e43330c416569c`.
The D1 freeze/orchestration triplet is bound at
`59f00d83aba2545ec80b4778cfa181b0a5a0be043bddfb42aef212aaf7533e6d`,
`11d341f65bde47caffcf3c946919c3c0c83254684fb58d0ad643b1874fb3a973`,
`fda8bafabcb8737035d0b342fd5639a6618900d0a958bd0dbbf0adb827ac0d25`,
and
`7cf3a5785f6bb3576357fe8c9bd867955660c2ff2486ca0710c1398e32b1cb0e`.

The additive E-A1-D1 human, machine, and test records are bound at raw
SHA-256 values
`bd00e6d145a5517ed8ecd34f6547c49d6d8d4eae67aeb8321037bf6ca54b3ba5`,
`b52685e2b61a30c5781f0e75138eaae6410063fa2312a447eeed7a4d1902cac0`,
and
`2c6ef628557c531b91c836113b9feb31e99ca48b4b7d16134c84998d739bd1e5`.
The E-A1-D1 machine self-digest remains
`d1c52907ba0bbb6b17cb2cb4e930d983623f39c161ad8a116afa43dccbbfa1b9`.
The D1 diagnostic record, success receipt, and consumed-attempt marker are
bound at raw SHA-256 values
`4b983cd0dcb0e068bfd6d8c47d726e2f02ecb6cf5e17b4f77022d1e10f8c7b10`,
`eabecf04bfe0831fa14d60126c541774aaf25c58283ebb999dc3de2403e9cada`,
and
`acfc404eca9ed711279087861518b7e9b32dfdb5fec4aaba318b50e7b4854e14`.

The exact source boundary is production-order source
`be2b4134672fc2895242d8cbb68d8c540345574f1b31ed8b04a50b88793235e1`,
rank-stress source
`ead7544be821d58874fd07d4293adc078257f8efb47a82a1e91ea2fa0b702c67`,
runtime-identity source
`ba10c8053796b6d36bc02a2bea0716a443bba35fd1190333292b87701eb18bf0`,
and environment lock
`ba373a4f7ef687e55d6f0a5cbc1f14eaf9db03ab1cf001cc8d6009e85adbbc5d`.

## 3. Exactly two pre-D1 fields are projected

The historical machine preregistration contains 174 unresolved `null` values:
168 pre-execution nulls and six deferred post-execution audit-plan nulls. This
successor projects exactly two pre-execution fields that the hash-bound A1
specification froze before D1:

1. `/theory_and_known_law_plan/a1_fixture_parameters`; and
2. `/theory_and_known_law_plan/a1_evaluation_grid`.

The A1 fixture projection is jointly sourced from the pre-D1 A1 scientific
specification and the pre-D1 V2 machine freeze. The latter attests the
production fixture identity that is not printed as a literal in the scientific
specification. The projection is: horizon `1`; cap `3`; three types; 20 states;
21 observations; overflow index `20`; birth rates
`(0.38, 0.30, 0.24)`; per-occurrence death rates
`(0.28, 0.34, 0.25)`; row-source/column-destination replacement matrix
`((0, 0.16, 0.07), (0.11, 0, 0.15), (0.09, 0.13, 0))`; initial
capped-factorial parameter `(0.65, 0.50, 0.40)`; detection probabilities
`(0.72, 0.63, 0.68)`; row-source confusion matrix
`((0.62, 0.25, 0.13), (0.22, 0.58, 0.20), (0.18, 0.27, 0.55))`;
observation-clutter rates `(0.10, 0.08, 0.12)`; uniform observation-reference
mass `1/21`; whole-observation contamination `0.08`; and production fixture
SHA-256
`0121b487728b40356de6707a33ba4881100c3d1b587259b19723463a60cecdcc`.
The machine sidecar stores every decimal as an exact string and cites both
source bindings for every projected field.

The A1 evaluation grid is exactly `t_j=j/32` for `j=0,...,32`: 33 ordered
rational points from `0/32` through `32/32`. The last point is a boundary
check and supplies no gradient. The machine sidecar enumerates all 33 rational
strings; it does not replace them with binary floating-point values.

The resulting effective unresolved count is therefore 172, comprising exactly
166 pre-execution nulls plus the same six deferred post-execution nulls. No other
historical null is treated as resolved. The six deferred fields remain
nonblocking for confirmatory execution but blocking for claim promotion and
submission, exactly as the historical preregistration states.

None of the 12 unresolved blockers is closed. All ten
`CONFIRMATORY_EXECUTION` blockers and both
`CLAIM_PROMOTION_AND_SUBMISSION_NOT_CONFIRMATORY_EXECUTION` blockers remain
open. No freeze-predicate condition is closed: every false or unresolved
condition remains false or unresolved, including the test-data-unopened value
of `null`. Resolving two leaf fields does not imply that any containing
scientific, implementation, power, domain, artifact, or review blocker is
complete.

## 4. Staged successor freeze—no stage skipping

The successor ordering is deliberately staged:

| Stage | Target state | Current state |
|---|---|---|
| R1 finite-A1 | `R1_A1_FROZEN_EXECUTABLE` | `NOT_EXECUTABLE` |
| R2 mixed hybrid | `R2_HYBRID_FROZEN_EXECUTABLE` | `LOCKED_WAITING_FOR_R1_RESULT_RECEIPT` |
| Both real domains | `REAL_DOMAIN_TEST_FROZEN_EXECUTABLE` | `LOCKED_WAITING_FOR_R1_R2_AND_ALL_REAL_DOMAIN_GATES` |

The historical combined `r1-r2-known-law-gate-receipts` row cannot gate R1
without circularity: R1 cannot be required to produce its own post-run result
receipt before R1 execution. This successor therefore decomposes the receipt
sequence. R1 must first receive its own complete **pre-execution freeze
receipt**, after a versioned scientific freeze, power/seed review, formal
runtime identity, typed runner integration, and production-plan and
phase-custody binding; R1 does not depend on an R1 post-run result receipt or a
combined R1/R2 result receipt. R2 remains locked until an accepted R1 **result
receipt** exists and R2 has its own pre-execution freeze receipt. Real-domain
test access remains locked until accepted R1 and R2 **result receipts**, a
real-domain pre-execution freeze receipt, and every domain, leakage, metric,
power, governance, compute, and test-access gate are frozen. This closure
supplies no receipt for any stage and permits no stage skipping.

## 5. D1 exposure and anti-selection boundary

D1 observed `(seed=1729, accepted-example budget=32768, method=guided)`. That
coordinate lies inside the current primary grid. Because D1 is prior observed
development knowledge, the whole seed `1729` is conservatively designated
`PILOT_NONCONFIRMATORY_EXPOSED` with a fail-closed wildcard across
**every lane, method, and budget**, using a budget wildcard wherever budget is
not applicable. This includes exact, primary, and control coordinates and is
not limited to the observed coordinate or to primary methods. Any enumerated
current-grid coordinates in the machine record are illustrative audited
subsets, never an exhaustive definition of the exposure boundary.

Seed `1729` is not eligible for a future confirmatory decision. This closure
does not select a seven-seed confirmatory design, reduce the schedule from
eight to seven, choose a replacement seed, or freeze a confirmatory seed
count. It also does not use D1 to define an R1 success rule or select a metric,
threshold, checkpoint, seed, seed count, or overflow policy.
D1 may not change the future overflow policy. Overflow exclusion is not
permitted. A later
R1 freeze must cite the D1 diagnostic-record raw SHA-256
`4b983cd0dcb0e068bfd6d8c47d726e2f02ecb6cf5e17b4f77022d1e10f8c7b10`
and the E-A1-D1 registration raw SHA-256
`b52685e2b61a30c5781f0e75138eaae6410063fa2312a447eeed7a4d1902cac0`,
declare all post-D1 design controls, and obtain a non-outcome-driven power and
seed review before choosing a confirmatory schedule.

## 6. Static rank-prefix qualification and absent authority

The bound production-order source still declares
`production_order_authority=true`,
`production_execution_authority=false`,
`runner_integration_complete=false`, `test_only_no_run=false`, and
`opaque_evidence_admission_allowed=false`. Its phase-consumption record still
requires `production_execution_permit_issued=false`,
`runner_binding_complete=false`, and
`scientific_execution_authorized=false`. Its only implemented evidence
transitions reach `RANK_AUTHORIZED`; they do not authorize a coordinate.
Coordinate issuance structurally refuses unless all three phase-consumption
execution gates are true.

The source-level `test_only_no_run=false` value is recorded exactly; it does
not override the false execution gates. No production plan artifact exists,
no actual rank phase is opened or authorized, the production order is not
admissible for execution, no phase has been consumed, and no runner binder is
complete. The rank-stress
launcher symbol is present in the bound source but was inspected only as
syntax; it was not imported or called, and no rank result was loaded or
computed.

The formal runtime-identity manifest
`requirements/m1-reference-macos-arm64-py311.runtime-identity.json` is absent.
Both protected roots—`artifacts/a1_campaign_v4` and
`artifacts/a1_finite_association_production_order_v1`—are absent. The rank
gate files, exact-population campaign, primary-metrics roots, candidate
decision, independent audit, and publication decision are also all absent.
Their absence is a blocker-preserving observation, not evidence of a
successful run.

The qualification module has one closed-world input: an absolute workspace
root. Its only public operations return the deterministic static audit or its
status label. It accepts no checkpoint, evidence payload, runtime report,
plan, permit, output path, or replay token; contains no project-artifact write
primitive; imports no project execution module; and offers no launch or
compute operation. Qualification and focused-test imports must run with Python
bytecode writing disabled so interpreter cache creation does not add a fifth
package artifact.

The closed-world path list is a static-audit input inventory, not an R1 input
allowlist. V1, V2, and D1 files are reopened only to verify custody and record
prior development knowledge. Their roots, checkpoints, failure/success
receipts, diagnostic record, attempt marker, metrics, and results are excluded
from R1 execution inputs and cannot be relabeled as production checkpoints or
R1 results.

## 7. Claim, readiness, review, and publication preservation

The global execution preregistration remains `DRAFT_NOT_EXECUTABLE` and
confirmatory execution remains unauthorized. `C17` remains
`THEOREM-TARGET`, not proved or closed. `R1-A1` and `R2-HYBRID` remain
`NOT RUN` with empty results. No claim is promoted, no manuscript result is
created, no production or scientific result is admitted, and CP76 receives no
readiness transition. The CP76 manifest and its focused test are directly
hash-bound here, but `NOT_READY` is the preserved historical snapshot state,
not a fresh live readiness recomputation. This package does not mutate that
snapshot.

All four raw artifacts in this additive package—the human closure, machine
sidecar, static qualification module, and focused test—are internal records,
not anonymous submission artifacts. None is authorized for anonymous-
submission or public-release inclusion. The prior E-A1-D1 publication/
anonymity restrictions also remain in force: no immutable custody artifact may
be sanitized in place, the publication-safe derivative path remains `null`,
the inclusion roster remains unfrozen, and a fresh publication/anonymity audit
remains required for both this package and its development predecessors.

No external-review panel is needed to record this nonpromotional closure.
Future proof, methods/statistics, reproduction, claim-promotion, and submission
reviews remain required at their preregistered stages. This static
qualification is not one of those reviews and cannot substitute for any of
them.

## 8. Next executable-boundary work

The next honest work is still pre-execution work: a versioned R1 scientific
freeze must resolve the remaining R1 metric, threshold, failure, power, and
seed decisions without using D1 outcomes; define the treatment of every
exposed seed; bind a formal runtime-identity manifest; complete a typed
production-order-to-runner integration; and issue a freeze receipt only after
all R1 execution blockers and required artifacts pass. Until then, the global
and R1 states remain non-executable.
