# Association-Aware Bridges for Heterogeneous Event Configurations

> **Additive locked-route successor v1 — pre-outcome, result-free**  
> **State:** `MANUSCRIPT_V3_LOCKED_ROUTE_SUCCESSOR_PREOUTCOME`  
> **Scientific execution:** none  
> **Claim promotion:** none

## Successor and historical boundary

This document is the current method/domain synchronization layer for the
historical manuscript `manuscript_v3/manuscript_v3.md`. The historical file is
retained byte-for-byte at SHA-256
`0569b18aefb2aefa6c24af0559880f66c4a0daa6b2073169d30c892515e976a8`.
It remains evidence of the earlier draft, not the current route authority.

The synchronized TeX companion is
`manuscript_v3/manuscript_v3_locked_route_successor_v1.tex`; current claim
states are in `manuscript_v3/claim_ledger_locked_route_successor_v1.md`.
Together they supersede only stale method, metric, domain, contribution, and
status passages. They do not rewrite or reverse-bind any historical file.
Where a historical passage conflicts with this successor, this successor
controls prospectively. Historical validators continue to validate historical
bytes only.

This is an internal project-control manuscript successor. It is not yet an
anonymous submission derivative and contains no result prose.

## Abstract — synchronized result-free scope

We study conditional generation of capped, unordered configurations of typed
events with type-dependent continuous marks, physical time, multiplicity, and
partially observed occurrences whose association with latent events is
unknown. The mathematical object is a finite counting measure on a typed event
space. Its reference dynamics combine within-type diffusion with birth, death,
and type-changing replacement. Conditioning is represented by a positive
backward-information function; a computable association-aware guide and a
learned residual are candidate implementation components.

The bridge and Doob-transform machinery is not claimed as novel. The current
minimum contribution route is a high-quality empirical benchmark and
association-mechanism evaluation on two real domains, with no novel-method or
novel-mechanism claim. That route is still pending every one of its fourteen
conjunctive evidence components. The generic count-normalized-event CKS
construction has a proved generic theorem and a validated finite-alphabet
reference implementation, but no exact PhysioNet or Retail metric instance is
yet admitted. C17 remains unproved, and real-domain C17 promotion under the
current observability route is `NO_GO`. R1, R2, R3, and R4 have no scientific
result. No alternate domain, metric, theory headline, or Route-B fallback may
replace a failed locked-route component.

## 1. Exact current contribution boundary

The only current prospective empirical route is

`HIGH_QUALITY_EMPIRICAL_BENCHMARK_AND_ASSOCIATION_MECHANISM_EVALUATION_WITH_NO_NOVEL_MECHANISM_CLAIM`.

Its stages are:

1. `R1-A1`: the finite association-guided known-law route;
2. `R2-HYBRID`: the occurrence-marked mixed CTMC--OU known-law route;
3. `R3-PHYS`: the admitted PhysioNet confirmatory domain; and
4. `R4-RETAIL`: the admitted Online Retail II confirmatory domain.

All four are pending or not run. There is no current `R5-ASAP`, three-domain
generality route, music-domain rescue, alternate primary score, physical-time
Route B, or post-outcome fallback. ASAP and MAESTRO may appear only as
historical/source-reproduction context unless a genuinely separate future
project receives new prospective authority and a nonoverlapping outcome
boundary.

The empirical contribution predicate is conjunctive. It requires valid R1 and
R2 passes; prospective admission of both real domains; a matched-compute
baseline suite; separate primary-effect, no-regression, and
association-mechanism-contrast passes in each real domain; precommitted
known-law and two-domain scaling; complete failure accounting under frozen
ceilings; and clean-room reproduction. One domain, secondary metric, seed,
baseline, theory route, or successful subset cannot rescue another component.
Any component-level `TERMINAL_NO_GO` is absorbing for this route.

## 2. Configuration and hybrid-process contract

An admissible state is a capped finite counting measure

\[
x=\sum_{i=1}^{n}\delta_{e_i},\qquad 0\le n\le N,
\]

on a typed event space. Order is quotiented out; repeated occurrences and exact
ties retain multiplicity. Type determines the legal continuous mark chart.
The dominating configuration measure uses the canonical symmetrized
`1/n!` convention. Birth, death, and replacement intensities are aggregate
unlabeled-configuration rates and therefore include the applicable source
multiplicity, destination-fiber Radon--Nikodym factor, Jacobian, and cap rule.
Structural-zero and cap-blocked edges have zero legal flux and no invented log
ratio.

The reference process combines:

- within-type diffusion on each legal continuous chart;
- births with a declared destination mark fiber;
- per-occurrence deaths;
- type-changing replacements with a declared mark kernel; and
- an explicit cap with separately audited boundary flux.

For a positive backward-information function `h`, the conditioned continuous
drift adds `a_bar grad log h`, while each legal jump family is multiplied by
the correctly oriented destination/source information ratio after base
multiplicity and measure factors are applied. The candidate guide `hat h`
must preserve support. Initialization is the full normalized tilted law, not a
base-law shortcut, and endpoint validation is the candidate-owned propagated
law, not a terminal-potential check or moments-only proxy.

The repository contains many independently qualified component packages. No
fixed count such as “six implementation layers” is current authority. Formal
whole-method admission still depends on unresolved tests and frozen numerical,
runtime, scaling, failure, and domain contracts.

## 3. Association guide, residual, nuisance, and cap ownership

The association observation law marginalizes every compatible occurrence-to-
observation bijection under one normalized convention. Factorized or
best-matching substitutes are controls, not the same guide. A classifier logit
may decompose as

\[
\ell=\log h+C(A,M,Z),
\]

where the nuisance is constant in process time and process state for the fixed
conditioning branch. It must not be folded into `h` or `hat h`; quotienting
removes it from spatial gradients, legal-edge log differences, and normalized
initialization, while gauge-fixed equal-prior calibration remains able to
detect an unremoved shift.

The exact-versus-plug-in shared-guide path identity has exactly five owners:

\[
K_{\mathrm{path}}=K_0+K_C+K_{\mathrm{birth}}
 +K_{\mathrm{death}}+K_{\mathrm{replacement}}.
\]

A cap/reference-guide defect is not a sixth path-KL summand. It may enter a
future C17 end-to-end argument through exactly one explicitly selected and
proved route. Target/reference mismatch, analytic-guide approximation,
residual estimation, cap restriction/defect, initialization,
terminal-reference mismatch, and numerical enclosure are distinct accounting
buckets and may not be double counted. Numerical enclosure width is an
arithmetic control, not scientific error.

## 4. C17 and initializer-KL status

PO13 is proved for positive finite normalizers. If

\[
dP_0=\frac{h}{Z_h}d\rho,\qquad
dQ_0=\frac{\widehat h}{Z_{\widehat h}}d\rho,\qquad
e=\log(\widehat h/h),
\]

then, with target-first orientation,

\[
\operatorname{KL}(P_0\Vert Q_0)
=\log\mathbb E_{P_0}[\exp(e)]-\mathbb E_{P_0}[e].
\]

This discharges the conditional initializer derivation and orientation only.
It supplies no real-domain error range, numerical `U_0`, finite nonvacuous
C17 bound, or C17 theorem.

The current disposition is:

`REAL_DOMAIN_C17_PROMOTION_UNDER_CURRENT_FORK_B_OBSERVABILITY = NO_GO`.

C17 is not a required real-domain headline and is not a conjunct of the
minimum empirical-contribution predicate. It may remain only an unproved
specification, a conditional theorem target, or a finite/mixed known-law
falsification route. It may not be counted as real-domain evidence, a
model-quality guarantee, a novelty claim, or a rescue after an empirical
failure.

## 5. Locked CKS construction and remaining instance gate

The historical raw, unnormalized embedding
`mu_x = integral k_E(e,.) x(de)` is retired for the current route. Let
`n_x=x(E)`. For `n_x>0`, define the empirical probability
`p_x=x/n_x`; do not invent `p_x` for the empty configuration. With fixed
positive scales `a,b`, define the orthogonal direct-sum embedding

\[
\Phi(x)=\bigl(a n_x,\,b m_{p_x}\bigr)\quad(n_x>0),
\qquad \Phi(0)=(0,0),
\]

and, for fixed finite `sigma>0`,

\[
k_\Gamma(x,x')=
\exp\!\left[-\frac{\lVert\Phi(x)-\Phi(x')\rVert^2}{2\sigma^2}\right].
\]

Under the proved generic assumptions—standard-Borel event space, bounded
measurable characteristic event kernel with separable RKHS, injective event
representation, positive count/event scales, finite positive outer bandwidth,
and the exact capped configuration sigma-algebra—`Phi` is injective and the
Gaussian pullback is characteristic on configuration laws.

For independent draws `X,X'~P`, the lower-is-better configuration kernel score
is

\[
\operatorname{CKS}(P,x)=
\mathbb E k_\Gamma(X,X')-2\mathbb E k_\Gamma(X,x),
\]

up to a target-only constant. The generic theorem and exact-rational
finite-alphabet reference implementation are project-control evidence only.
They do not select the exact PhysioNet or Retail event schema, transform,
kernel, scales, cap, bandwidth, conditional draw count, meaningful effect,
real--real floor, or confidence procedure. Until those fields and a domain
instance are prospectively bound, CKS is not an admitted production primary
metric. Failure to admit that exact instance is route `NO_GO`, not permission
to select another score after outcomes.

The frozen favorable direction is positive
`score_direct - score_guide`, because all admissible primary representations
are lower-is-better losses. The aggregation unit is the natural-group-weighted
paired mean; repeated conditional draws are Monte Carlo samples, not
independent replicates. Metric fitting is train-only. The two real-domain
family uses the frozen one-sided Holm rule at familywise alpha `1/20`.

## 6. Known-law gates and accepted B05 design-freeze boundary

R1 uses the finite association-aware route. R2 requires a capped mixed fixture
with a discrete CTMC and occurrence-attached OU marks conditional on the
discrete path, at least one type-changing edge, exact or certified
initial/endpoint/path KL and TV quantities, certified numerical truncation,
source/destination RN factors, multiplicity, cap flux, normalized association,
and nuisance invariance. Equal-prior classifier calibration is a separate
metric; it is not normalized evidence and is not a KL/TV aggregand.

The cap-one fixture uses an ordinary continuous-time CTMC with exponential
jump times, matrix-analytic uniformization, and invariant OU mark fibers. The
earlier fixed/atomic-time restriction is superseded and is not evidence for
this mixed quantitative path.

The additive composite B05 design passed final independent stopped-byte audit
under
`B05_F007_F018_ALL_OR_NOTHING_KNOWN_LAW_DESIGN_FREEZE_CERTIFIED`. It has a
cap-one quantitative path core and three disjoint cap-two structural subcases.
Those cap-two witnesses are structurally disjoint and non-substitutive; they
are not a cap-two quantitative path result and not a completed R2 gate. The
accepted all-or-nothing design freeze closes exactly `F007` through `F018` as
pre-outcome fields. It is now bound into this successor's current field map.

B05 itself remains open. `F114`--`F127`, `F149`, execution, R1, R2, C17,
every formal test, and every scientific result remain unresolved. Diagnostic
decimal values, binary64 residuals, library tolerances, reference-enclosure
widths, and candidate-enclosure widths are not scientific pass thresholds.
The nonzero terminal-matched residual witness is not a decision candidate.

## 7. Two-domain admission and manifests

The confirmatory domain roster is exactly PhysioNet Challenge 2012 and Online
Retail II. Both require pre-outcome source, license/governance, schema,
representation, observation, support, snapshot, split, task, metric, power,
and custody receipts. No real data, source documentation, license record,
snapshot, split, test set, or domain admission has been acquired by the local
draft packages.

The Retail task/schema and dual-domain snapshot/split manifest constructors
have only synthetic qualification. They create no real manifest and close no
domain field. The Retail temporal mapping rule `F060` is frozen as a
deterministic customer-disjoint exhaustive gap-pair rule. Allocation and power
field `F061` remains null and open; the historical synthetic `70/15/15`
instantiation is not the value of `F060` and does not populate `F061`.

Actual PhysioNet and Retail snapshots must precede assignment. Customer or
patient groups must be complete and disjoint; no post-snapshot exclusion,
resplit, top-up, migration, favorable retry, or replacement domain is allowed.

## 8. Statistical, failure, and execution policy

The two real-domain hypotheses are separate and noncompensatory. Their
one-sided p-values use the frozen two-domain Holm step-down family at alpha
`1/20`; joint target power is `9/10`. The confidence method, primary metric
instance, effect thresholds, conditional draw count, real--real floor,
failure-rate ceiling, and complete allocation remain open.

R1 phases run in the frozen order
`R1_RANK -> R1_EXACT -> R1_PRIMARY -> R1_METRICS -> R1_CONTROLS`.
Any `FAIL`, `HOLD`, `INFRA_ABORT`, protocol-invalid receipt, or missing
terminal receipt is terminal for the current route. `F148` is
`NEVER_TRUE_NO_INFRASTRUCTURE_RERUN`. There is no retry, resume, replacement,
extra seed, threshold change, compute top-up, alternate domain, route switch,
or favorable omission. `F149`, the admissible failure-rate ceiling, remains
open and must be frozen before execution.

The project is `DRAFT_NOT_EXECUTABLE`. The pre-execution closure qualifies a
static prefix only; it does not authorize scientific execution. R1 and R2 are
not qualified scientific results, R3 and R4 are not admitted, and test data
have not been acquired.

## 9. Implementation and formal-test status

Component qualifications do not equal whole-method admission:

- the finite-acyclic Test-29 route/lineage component is qualified, while
  Formal Test 29 remains `OPEN`;
- the supplied-input Test-30 coupling precursor is validated, while Formal
  Test 30 remains `PENDING`;
- the supplied-input Test-29/Test-30 single-macrostep composition is
  validated, while Formal Test 28 remains `OPEN`, Formal Test 29 remains
  `OPEN`, and Formal Test 30 remains `PENDING`.

These packages use synthetic or caller-supplied values. They do not establish
an ideal continuous Gaussian route law, unconditional liveness, a production
initializer, complete mixed-path coupling, runtime approval, or a scientific
result.

## 10. Current field and blocker projection

After the accepted all-or-nothing B05 design freeze, the authoritative
additive count is:

| Scope | Open | Closed |
|---|---:|---:|
| Pre-execution fields | 146 | 20 |
| Post-execution fields | 6 | 0 |
| Total fields | 152 | 20 |

The closed pre-execution fields are `F007`--`F018`, `F060`, `F106`, `F107`,
`F108`, `F113`, `F128`, `F129`, and `F148`. All twelve blockers remain open.
No formal test, scientific result slot, domain admission, Gate-A
venue/primary-claim item, B04, or B05 is closed. The twelve newly closed
design fields cannot be used to claim B05, R1, R2, or science complete.

## 11. Results and claims

No result prose is authorized. All R1--R4 result cells are empty. No
model-quality, cross-domain, clinical, privacy, commercial, perceptual,
generality, scalability, C17, novel-method, or venue-quality claim has been
earned. The current empirical route is pending all fourteen components.

A later public manuscript must be regenerated from admitted, hash-bound
scientific receipts and pass a fresh claim-ledger, citation, anonymity,
license, and disclosure review. This successor synchronizes route semantics;
it does not refresh external citations or promote any claim.
