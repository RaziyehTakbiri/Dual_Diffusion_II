\documentclass[11pt]{article}
\usepackage[margin=1in]{geometry}
\usepackage{amsmath,amssymb,booktabs,array}
\usepackage{xurl}
\usepackage{hyperref}
\newcommand{\state}[1]{\texttt{#1}}
\title{Association-Aware Bridges for Heterogeneous Event Configurations\\
Locked-Route Successor v1}
\author{Withheld in the internal pre-outcome draft}
\date{2026-08-31}

\begin{document}
\maketitle

\begin{center}
\state{MANUSCRIPT\_V3\_LOCKED\_ROUTE\_SUCCESSOR\_PREOUTCOME}\\
Scientific execution: none. Claim promotion: none.
\end{center}

\section{Additive successor boundary}

This document is the current method/domain synchronization layer for the
historical \path{manuscript_v3.tex}, retained byte-for-byte at SHA-256
\path{0ad9abccbc38ccc41e9fb3f7a1f8db6a4a197d23c3946da60a3cd4b93b475ba9}.
The historical file remains evidence of an earlier draft, not current route
authority. This TeX file, its Markdown companion, and the locked-route claim
ledger supersede only conflicting method, metric, domain, contribution, and
status passages. They neither modify nor reverse-bind the historical files.
This is an internal result-free project-control successor, not a submission
derivative.

\section{Synchronized scope}

We study conditional generation of capped unordered configurations of typed
events with type-dependent continuous marks, physical time, multiplicity, and
unknown association between observed and latent occurrences. The reference
dynamics combine within-type diffusion with birth, death, and type-changing
replacement. Conditioning uses a positive backward-information function; a
computable association-aware guide and learned residual remain candidate
implementation components.

The generic bridge and Doob-transform machinery is not claimed as novel. The
only current minimum route is a high-quality empirical benchmark and
association-mechanism evaluation with no novel-method or novel-mechanism
claim. Its slots are exactly \state{R1-A1}, \state{R2-HYBRID},
\state{R3-PHYS}, and \state{R4-RETAIL}. All are pending or not run. There is no
current ASAP/R5 route, three-domain generality requirement, physical-time
Route B, alternate primary score, music rescue, or post-outcome fallback.

The empirical predicate is conjunctive: valid R1 and R2; admission of both
real domains; matched-compute baselines; separate effect, no-regression, and
association-mechanism passes in both domains; precommitted known-law and
two-domain scaling; full failure accounting under frozen ceilings; and clean-
room reproduction. One component cannot rescue another, and
\path{TERMINAL_NO_GO} is absorbing.

\section{Configuration and hybrid-process contract}

An admissible state is a capped finite counting measure
\[
x=\sum_{i=1}^{n}\delta_{e_i},\qquad 0\le n\le N.
\]
Order is quotiented out while repeated occurrences and exact ties retain
multiplicity. The configuration base uses the canonical symmetrized
$1/n!$ convention. Aggregate birth, death, and replacement rates include the
applicable source multiplicity, destination-fiber Radon--Nikodym factor,
Jacobian, and cap rule. A structural-zero or cap-blocked edge has zero legal
flux and no invented log ratio.

For positive information $h$, the conditioned drift adds
$\bar a\nabla\log h$ and each legal jump rate receives the correctly oriented
destination/source information ratio after the base measure factors. A
candidate $\widehat h$ must preserve support. Initialization is the complete
normalized tilted law; endpoint validation is the candidate-owned propagated
law. A terminal-potential check or moments-only comparison is insufficient.

No fixed statement such as ``six implementation layers'' is current
authority. Component qualifications are recorded by named packages; whole-
method admission remains open.

\section{Association, nuisance, and cap accounting}

The observation law marginalizes every compatible occurrence-to-observation
bijection under one normalized convention. Factorized and best-matching forms
are controls. A classifier logit may be written
\[
\ell=\log h+C(A,M,Z),
\]
where the nuisance is constant in process time and state for a fixed
conditioning branch. It is not part of $h$ or $\widehat h$. Gauge quotienting
must leave gradients, legal-edge differences, and normalized initialization
unchanged, while gauge-fixed calibration detects a retained shift.

The shared-guide exact-versus-plug-in path identity is
\[
K_{\rm path}=K_0+K_C+K_{\rm birth}+K_{\rm death}+K_{\rm replacement}.
\]
A cap/reference-guide defect is not a sixth path-KL component. A future C17
argument may count it through exactly one explicitly selected proved route.
Target/reference mismatch, analytic-guide approximation, residual estimation,
cap restriction/defect, initialization, terminal-reference mismatch, and
numerical enclosure are distinct buckets. Enclosure width is arithmetic
control, not scientific error.

\section{C17 disposition}

For positive finite normalizers,
\[
dP_0=\frac{h}{Z_h}d\rho,\quad
dQ_0=\frac{\widehat h}{Z_{\widehat h}}d\rho,\quad
e=\log(\widehat h/h)
\]
imply the target-first initializer identity
\[
\operatorname{KL}(P_0\Vert Q_0)
=\log\mathbb E_{P_0}[\exp(e)]-\mathbb E_{P_0}[e].
\]
This proves PO13's conditional derivation and orientation only. It gives no
real-domain range, numerical $U_0$, or C17 theorem. The current disposition is
\[
\state{REAL\_DOMAIN\_C17\_PROMOTION\_UNDER\_CURRENT\_FORK\_B\_OBSERVABILITY
\;=\;NO\_GO}.
\]
C17 is unproved, not a required real-domain headline, and not a conjunct of the
minimum empirical contribution. It may remain only a conditional theorem
target, unproved specification, or finite/mixed known-law falsifier.

\section{Count-normalized-event CKS}

The historical raw unnormalized finite-measure embedding is retired. Let
$n_x=x(E)$ and, for $n_x>0$, let $p_x=x/n_x$. With fixed $a,b>0$,
\[
\Phi(x)=(a n_x,b m_{p_x})\quad(n_x>0),\qquad \Phi(0)=(0,0),
\]
in the orthogonal direct sum of the count channel and event RKHS. For finite
$\sigma>0$,
\[
k_\Gamma(x,x')=
\exp\!\left[-\frac{\|\Phi(x)-\Phi(x')\|^2}{2\sigma^2}\right].
\]
Under the proved generic assumptions, $\Phi$ is injective and this Gaussian
pullback is characteristic on capped configuration laws. The lower-is-better
score is
\[
\operatorname{CKS}(P,x)=
\mathbb E k_\Gamma(X,X')-2\mathbb E k_\Gamma(X,x),
\]
up to a target-only constant.

The generic theorem and exact-rational finite-alphabet reference are validated
project-control objects. They do not admit a PhysioNet or Retail metric
instance. Exact event schemas, transforms, kernels, scales, caps, bandwidths,
draw count, effect, real--real floor, and confidence procedure remain open.
Failure of that prospective instance is route no-go, not permission for
post-outcome metric substitution.

The frozen favorable direction is positive lower-is-better
\texttt{loss\_direct-loss\_guide}; aggregation is the natural-group-weighted
paired mean; metric fitting is train-only; and the PhysioNet/Retail family uses
one-sided Holm control at exact alpha $1/20$.

\section{Known-law and accepted B05 design-freeze boundary}

R2 requires a capped CTMC with occurrence-attached OU marks conditional on the
discrete path, a type-changing edge, certified truncation, exact or certified
initial/endpoint/path KL and TV, source/destination RN factors, multiplicity,
cap flux, association normalization, and nuisance invariance. Equal-prior
classifier calibration is separate from normalized evidence and is not a
KL/TV aggregand.

The cap-one fixture uses an ordinary continuous-time CTMC with exponential
jump times, matrix-analytic uniformization, and invariant OU mark fibers. The
earlier fixed/atomic-time restriction is superseded and is not evidence for
this mixed quantitative path.

The composite B05 design passed final independent stopped-byte audit under
\path{B05_F007_F018_ALL_OR_NOTHING_KNOWN_LAW_DESIGN_FREEZE_CERTIFIED}.
It has a cap-one quantitative path core and three disjoint cap-two structural
subcases. Those witnesses are structurally disjoint and non-substitutive; they
are not a cap-two quantitative path result or completed R2 gate. The accepted
all-or-nothing design freeze closes exactly \state{F007--F018} as pre-outcome
fields and is bound into the current field map.

B05, \state{F114--F127}, \state{F149}, execution, R1, R2, C17, every formal
test, and every result remain open. Diagnostic decimals, binary64 residuals,
library tolerances, reference-enclosure widths, and candidate-enclosure widths
are not scientific pass thresholds. The nonzero terminal-matched residual
witness is not a decision candidate.

\section{Two real domains only}

The confirmatory roster is exactly PhysioNet Challenge 2012 and Online Retail
II. Both need prospective source, license/governance, representation,
observation, support, snapshot, split, task, metric, power, and custody
receipts. No real source, data, snapshot, split, test set, or domain admission
has been acquired by the draft packages.

Retail task/schema and dual-domain manifest constructors have synthetic
qualification only. The deterministic customer-disjoint temporal rule
\state{F060} is frozen. Allocation/power field \state{F061} remains null and
open; a synthetic $70/15/15$ fixture neither fills it nor changes F060.
No exclusion, resplit, top-up, retry, migration, or replacement domain is
allowed.

\section{Failure and formal-test status}

R1 follows
\path{R1_RANK->R1_EXACT->R1_PRIMARY->R1_METRICS->R1_CONTROLS}.
Any fail, hold, infrastructure abort, invalid receipt, or missing terminal
receipt is terminal. \path{F148=NEVER_TRUE_NO_INFRASTRUCTURE_RERUN};
\state{F149} remains open. There is no retry, resume, extra seed, threshold
change, compute top-up, route switch, or favorable omission.

The finite-acyclic Test-29 component is qualified while Formal Test 29 is open.
The supplied-input Test-30 precursor is validated while Formal Test 30 is
pending. Their supplied-input single-macrostep composition is validated while
Formal Tests 28 and 29 remain open and Test 30 remains pending. These are not
runtime approval or scientific results.

\section{Current projection and nonclaims}

After the accepted B05 design freeze there are 146 open and 20 closed
pre-execution fields, six open and zero closed post-execution fields, and 152
open and 20 closed total. The closed pre-execution fields are
\state{F007--F018,F060,F106,F107,F108,F113,F128,F129,F148}. All twelve
blockers, B04, B05, every formal test, every result, both domain admissions,
and the Gate-A venue/primary-claim item remain open. These twelve design-field
closures are not an R2 pass, a B05 closure, or a scientific result.

There is no model-quality, cross-domain, clinical, privacy, commercial,
perceptual, generality, scalability, C17, novel-method, or venue-quality
result. A future public manuscript requires admitted scientific receipts and
fresh claim, citation, anonymity, license, and disclosure review.

\end{document}
