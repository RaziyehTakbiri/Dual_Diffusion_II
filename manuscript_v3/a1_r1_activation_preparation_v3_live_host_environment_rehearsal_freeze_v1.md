# A1 R1 activation-preparation V3 live-host environment rehearsal freeze

## Status

This additive, internal registration freezes a read-only V3 environment
rehearsal.  Its state is
`R1_A1_ACTIVATION_PREPARATION_V3_LIVE_HOST_ENVIRONMENT_REHEARSAL_IMPLEMENTATION_FROZEN_AWAITING_SINGLE_READ_ONLY_REHEARSAL_NO_MARKER_NOT_EXECUTABLE`.
The global state remains `DRAFT_NOT_EXECUTABLE`.

No canonical or durable V3 marker, preparation root, nonce, capsule, request,
result, runtime candidate, review, or approval was created while freezing this
package.  No live rehearsal child was launched.  No entropy, network, rank,
training, production, or scientific route was invoked.  The hostile suite
uses synthetic in-memory observations and isolated pytest temporary-file
copies for tamper checks; those copies are not canonical or operational V3
outputs.  Process, entropy, and network entry points are patched to fail.

The spent V2 marker and terminal tree are immutable evidence.  V2 is not
retried, repaired, deleted, or resumed.  The four-file V2 terminal-failure
registration and its independently reopened three-event terminal prefix are
bound as the predecessor.  The V3 operational namespace is wholly disjoint:

- future marker: `artifacts/a1_r1_activation_preparation_v3.attempt.json`;
- future preparation root: `artifacts/a1_r1_activation_preparation_v3/`.

Both are absent now.  This package implements neither path.

## Authorization provenance and scope

The normalized conversation-visible user assent is exactly `Okay, go through
it.`  It followed the stated antecedent of a wholly disjoint V3 static package
and no-write live-host environment rehearsal with no marker.  Transport-level
trailing whitespace or an HTML entity is normalized; raw message transport
bytes are not bound as a registered workspace artifact.  This internal
registration binds the normalized visible text and a domain-separated digest.
The assent authorizes this six-file static freeze and independent audit, one
audited read-only live-host rehearsal after GO, and mandatory additive
publication of its exact PASS or FAIL result.  It authorizes no retry and no
V3 marker, root, nonce, capsule, runtime approval, rank, training, production,
or scientific action.  Any future marker requires a separate writer design
and fresh exact user authorization.

## Exact trust split

The six additive files are this human registration, its machine sidecar, a
strict contract module, a read-only supervisor, a dedicated environment-only
child, and a hostile test.  The child imports only the standard library.  It
does not import the V2 oracle, a capsule, numerical libraries, project source,
or any launcher.  The supervisor has no filesystem writer, entropy source,
marker constructor, capsule materializer, authority ledger, runtime-approval
route, or scientific entry point.

Before a future child can start, the supervisor must authenticate the machine
record, the independently pinned contract and child bytes, all six static V3
files, the canonical workspace inode anchor, and the exact V2 terminal
custody.  The machine registration itself is included in the preflight and
postflight protected roster.  The workspace anchor contains internal device,
inode, and mode custody only; it contains no user path, UID, GID, or Darwin
encoding value and is excluded from publication.

## Rehearsal profile

The canonical parent command is `/usr/bin/env -i`, followed by the frozen
environment below and the pinned M1 Python with flags in this exact order:
`-P -B -S -X utf8`.  The parent launches exactly one copy of the dedicated
child with the same environment and flag profile.  Neither `-I` nor `-E` is
used: those flags would ignore `PYTHONHASHSEED=0` and invalidate a claim that
the seed-zero hash fixture was exercised.  `-s` is unnecessary because the
honored `PYTHONNOUSERSITE=1` must yield `no_user_site=1`; `-S` must yield
`no_site=1`, and importing `site` is a typed failure.

The exact requested environment has 16 keys:

1. `BLIS_NUM_THREADS=1`
2. `CUDA_VISIBLE_DEVICES=`
3. `LANG=C`
4. `LC_ALL=C`
5. `MKL_NUM_THREADS=1`
6. `NUMEXPR_NUM_THREADS=1`
7. `OMP_NUM_THREADS=1`
8. `OPENBLAS_NUM_THREADS=1`
9. `PYTHONDONTWRITEBYTECODE=1`
10. `PYTHONHASHSEED=0`
11. `PYTHONNOUSERSITE=1`
12. `PYTHONPYCACHEPREFIX=/dev/null`
13. `PYTHONSAFEPATH=1`
14. `PYTHONUTF8=1`
15. `TZ=UTC`
16. `VECLIB_MAXIMUM_THREADS=1`

`PATH`, `HOME`, `PYTHONPATH`, `PYTHONHOME`, credential variables, and `DYLD_*`
variables are absent.  On Darwin, the effective process environment is
expected to contain exactly these 16 keys plus the platform-injected user-text
encoding key.  The child derives the expected value in memory from
`os.getuid()`, requires real and effective UID equality, real and effective GID
equality, non-root identity, absence of supplemental group zero, and a false
`issetugid()` taint flag.  It validates the captured effective 17-key profile,
deletes the injected entry from the actual child-process `os.environ`, verifies
that the actual environment is then exactly the requested 16-key mapping, and
only then constructs its observation.  The raw value, its digest, and all
numeric identity values are forbidden from stdout, the result, the machine
registration, this human file, and any publication derivative.

The probe also requires CPython 3.11.5, 64-bit Darwin arm64, the pinned venv
executable and framework realpath, the exact standard-library-only `sys.path`,
the canonical workspace working directory, direct-file `__main__`,
`__spec__ is None`, exact Python and native argv, `hash_randomization=0`,
`ignore_environment=0`, `isolated=0`, `safe_path=true`, no bytecode, UTF-8
mode, and `/dev/null` as the cache prefix.

## Prefrozen hash reference

The seed-zero reference was fixed before any live V3 rehearsal.  For CPython
3.11.5 on 64-bit Darwin arm64, the ordered strings `heterodiff-a1`,
`dual-manifold`, `runtime-attestor`, and `frozen-prerequisite-v2` map to the
signed integer vector registered in the internal machine sidecar.  Its
canonical no-space, no-LF ASCII JSON preimage is 82 bytes.  Direct SHA-256,
with deliberately no domain separator for this legacy-compatible reference,
is
`f7b1ba1308d7559c69fc44640d0fcd07dbeae53b9024da5d862463db71e230af`.
The reference was derived before the run from CPython 3.11 SipHash13 with the
seed-zero all-zero secret; no live Python/profile rehearsal was used to select
it.  A future result exposes only this digest and its match Boolean, never raw
identity/environment data.

## Output and failure semantics

The child writes one bounded canonical ASCII JSON observation to stdout and
requires empty stderr.  The parent uses bounded pipes, a 30-second timeout,
`close_fds=true`, stdin EOF after the exact request pipe, a replacement
environment, and a new process session.  It never logs raw child stdout or
stderr.  A valid
parent result embeds the complete privacy-safe observation, its raw/self
digests, exact byte count, and transport outcome.  It binds the deterministic
request by its recomputed raw/self digests, plus workspace-anchor identity,
protected roster, and pre/post custody digests; it does not embed the request
object.

The result schema is one discriminated `PASS|FAIL` record.  Failure priority is
fixed, and validators recompute it from typed gates rather than trusting a
label.  Environment-key drift, Darwin normalization, identity/taint, platform,
flags, hash, path, `site`, bounded transport, and child-process failures are
representable without raw values.  A PASS requires every supervisor, child,
custody, absence, and non-effect gate.  Unsafe Boolean states may be encoded as
FAIL by the generic schema but are not admitted by this freeze's result loader;
the canonical route can publish only results with exact supervisor and
protected-custody gates and no marker, approval, write, entropy, network, or
scientific effect.

There is no durable one-shot file before publication.  Therefore
`mechanical_one_shot_enforced=false` and
`prepublication_replay_resistance=false`.  The independent procedure permits
one canonical parent and at most one child, with `retry_count=0`; a failure is
recorded as-is and requires a separately versioned design, not selection or a
retry.  A static-registration or supervisor-admission failure occurs before a
trusted typed result can be constructed.  A postflight audit exception or
loss of the stdout transport can likewise prevent the result from being
bound.  Any such case is terminal for this rehearsal version and requires a
separate additive postmortem.  This limitation is explicit; the package does
not claim a sole-writer or adversarial security boundary.

The rehearsal itself writes no application workspace, temporary, cache,
marker, or output-root file.  Standard output and internal tool logs may exist,
and the OS may update metadata; “read-only” is scoped to application-level
workspace/temporary/output creation and mutation.  After an independently
audited single run, the exact stdout bytes must be added once, without editing
these six prefreeze files, at
`research/fixtures/manuscript_v3_a1_r1_activation_preparation_v3_live_host_environment_rehearsal_result_v1.json`.

All six static files, the future exact result, V2 custody, the workspace-anchor
identity, and internal tool or conversation logs are internal evidence and are
excluded from anonymous or public release.  A publication-safe derivative
requires a fresh anonymity audit and must omit the internal workspace anchor,
all UID-like or Darwin-injected data, and any other identifying custody.

## Transition handling

Before the rehearsal result exists, the status is the pre-run state above.
An exact admitted PASS result changes only the rehearsal evidence state to
`R1_A1_ACTIVATION_PREPARATION_V3_LIVE_HOST_ENVIRONMENT_REHEARSAL_PASSED_RESULT_FROZEN_NO_MARKER_NOT_EXECUTABLE`.
An exact admitted FAIL result changes it to
`R1_A1_ACTIVATION_PREPARATION_V3_LIVE_HOST_ENVIRONMENT_REHEARSAL_FAILED_RESULT_FROZEN_MARKER_INELIGIBLE_NOT_EXECUTABLE`.
An invalid result or an unvalidated future V3 namespace is reported fail-closed
rather than breaking or silently blessing the static registration.  This
makes the status and tests transition-aware without treating a future marker
as valid before its additive validator exists.

A PASS qualifies only propagation of this exact environment/interpreter
profile.  It is not capsule admission, runtime identity approval, numerical
library determinism, executable preregistration, activation, or scientific
authority.  The BLAS/CUDA/thread keys are checked as environment strings; no
numerical library is imported to assess them.

## Preserved state and next gate

The frozen projection remains 172 unresolved nulls, 12 open blockers, and a
550-row D1 quarantine roster.  D1 remains non-admissible.  No scientific
campaign nonce exists.  Rank, training, production, scientific execution,
claim promotion, and submission remain unauthorized.

The next gate is an independent exact-byte and hostile-test audit of this
six-file package.  Only after GO may the single read-only rehearsal be run.
Its exact PASS or FAIL stdout must then be published additively.  Even a PASS
does not authorize a V3 marker: a later disjoint V3 writer design and fresh,
exact, informed user authorization remain mandatory.
