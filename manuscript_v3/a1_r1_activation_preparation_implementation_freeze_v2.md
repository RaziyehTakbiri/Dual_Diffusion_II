# A1 R1 activation-preparation implementation freeze v2

## Status and scope

This additive internal record freezes a preparation-only implementation. Its exact state is
`R1_A1_ACTIVATION_PREPARATION_IMPLEMENTATION_V2_FROZEN_AWAITING_EXPLICIT_MARKER_AUTHORIZATION_ZERO_EXECUTION_NOT_EXECUTABLE`.
The global manuscript state remains `DRAFT_NOT_EXECUTABLE`: 172 unresolved null values and all 12 blockers remain open. No executable preregistration or prerequisite-evidence receipt exists.

This milestone supersedes only the dormant v1 activation-path design. It does not alter any historical source, preregistration, diagnostic, checkpoint, artifact, claim, or result. All 17 dormant-v1 future paths and the old planned workspace `src/` adapter path remain permanently absent.

No canonical/live v2 marker or root was created; no live entropy, preparation-instance nonce, or real child inspection occurred. The focused suite exercised deterministic synthetic marker, capsule, and runtime-candidate custody only inside temporary replicas, using fixed bytes while entropy and child-process launch were patched to fail. No scientific campaign nonce exists. No rank, training, production, scientific, result-selection, or network operation was run.

## Six additive files

The package contains exactly this human registration, one canonical machine registration, three standard-library implementation modules, and one hostile test. The machine record binds the final exact bytes of the other five files without a digest cycle.

The nine closed record schemas are the v2 attempt marker, ledger genesis, ledger event, operation-nonce claim, source-capsule manifest, source-capsule admission, runtime double-capture request, runtime envelope binding, and runtime candidate. Every record has an exact key set, exact types, canonical ASCII JSON, one domain-separated terminal digest, and LF termination.

## New namespace and immutable predecessor boundary

The only marker path is `artifacts/a1_r1_activation_preparation_v2.attempt.json`. The only preparation root is `artifacts/a1_r1_activation_preparation_v2/`, containing `ledger/`, `capsule/`, and `runtime-candidate/`. The runtime-candidate directory may contain only `candidate.json`; raw capture envelopes, runtime review, runtime approval, and final-runtime-manifest files are forbidden.

The v2 status/audit is transition-aware. Before the marker, it proves that the v2 marker and root are absent. After a future authorized transition, it validates only exact state-machine prefixes, immutable record links, file and directory types and modes, the complete ledger roster, and capsule/runtime custody. It does not keep treating historical pre-marker absence as a current fact. An empty, partial, oversized, linked, wrong-mode, nonregular, or otherwise invalid marker is a terminal spent attempt with no retry.

## Irreversible authorization boundary

The exact authorization context frozen for a future user decision is:

> I authorize the irreversible A1 R1 activation-preparation v2 sequence: its one-shot marker, deterministic source-capsule materialization, and exactly two privacy-safe runtime inspections; this does not authorize runtime approval, rank, training, production, scientific execution, claim promotion, or activation.

This registration records that authorization as required; it does not assert that it has been given. The live marker route is a direct-file, canonical-workspace, isolated `-I -S -B` CLI only. It checks Python and native process arguments, interpreter identity, current directory, source identity, every dormant-v1 absence gate, and the complete current v2 prefix. Ordinary import, alternate-root, `runpy`, Python-vector forgery, and path-alias calls cannot reach canonical writes. These are procedural honest-host safeguards, not a sandbox or resistance against an actor who can mutate same-process memory, native argv memory, module globals, or registered files.

The marker inode is created with `O_EXCL` and `O_NOFOLLOW`, locked, set to mode 0600, fsynced, and its parent directory fsynced before the sole 32-byte `secrets.token_bytes` call. The raw bytes are domain-hashed into `preparation_instance_nonce_sha256` and discarded. They are never printed or persisted. Any marker inode—zero length, partial, valid, or invalid—spends the attempt and forbids retry. Only a preparation-instance nonce may be minted; a future scientific campaign nonce remains a separate executable-preregistration/activation responsibility.

## Preparation ledger

The parent authority is the sole filesystem writer. The immutable ledger has a genesis record and preparation events, separate from scientific authority ordinals 0 through 589:

1. `CAPSULE_MATERIALIZATION_OPENED`
2. `CAPSULE_ADMITTED`
3. `RUNTIME_DOUBLE_CAPTURE_OPENED`
4. either `RUNTIME_CANDIDATE_ADMITTED` or terminal `RUNTIME_DOUBLE_CAPTURE_REJECTED`
5. on the admitted branch only, `PREPARATION_CLOSED_AWAITING_OPERATOR_APPROVAL`

Genesis has no event ordinal. Each event uses its exact ordinal, immediate previous head, exact payload raw and record digests, and deterministic operation nonce. The ledger has no mutable head file. A full closed-world scan rejects gaps, extras, links, hard links, mode changes, reordered or orphaned claims, payloads without prior claims, mismatched payloads, and branch confusion.

Deterministic record-publication gaps may resume only within the same spent preparation attempt. Capsule files may resume only after event 0 and before the event-1 claim; already written rows must reopen with exact bytes. Once the event-1 claim or admission exists, capsule custody is immutable and missing data is terminal rather than repaired. A runtime slot claim is durable before its child launch. A claim without its matching privacy-safe binding is terminal for that slot and can never cause a third capture or recapture. A completed semantic mismatch terminates at event 3; it never emits the admitted event kind or event 4.

## Source capsule custody

The capsule plan contains exactly 53 files: the predecessor's 47 local package sources, three nonpackage inputs, and three dormant-v1 protocol copies. The parent authority is not copied into the child's import path. Five source files receive only the frozen one-literal ordinal-zero seed overlay; every overlaid module-level registry must equal
`(4052249444591756, 3253, 5003, 7411, 10007, 13007, 16001, 20011)`.
Seed 1729 is absent from every execution registry.

Materialization never edits workspace `src/`. Capsule directories are 0700 and files are 0600, regular, nonsymlink, and single-link. Admission performs two independent file-content reopens around full file/directory identity snapshots, rejects missing or extra files and directories, rejects `.pyc`, rederives all five overlay assignments, and records `ADMITTED_PREPARATION_CUSTODY_ONLY`. That is custody admission only, not execution admission.

## Exactly two privacy-safe runtime inspections

The registered v2 runtime module is a separate non-scientific inspection oracle outside the 53-file future-scientific capsule. It is registration-bound evidence code, not an execution-capsule input. It imports no parent authority, legacy launcher, or scientific project code. The authority reopens its exact source bytes immediately before and after each child and binds and checks the frozen API identifier.

The future preparation route allows exactly one A capture and one B capture under a complete replacement environment, isolated flags, the exact target interpreter/profile, the admitted capsule, and the exact installed-file root. Child stdout and stderr are hard-bounded; timeout, overflow, nonzero exit, or stderr is terminal. Each raw envelope is kept only in parent memory. Nothing writes or logs it.

Before discarding raw bytes, the parent validates the full typed nested observation, recomputes the canonical digests of the child-produced capsule, installed-file, and distribution rosters, independently reaudits the admitted 53-row capsule, checks target Python/platform/environment identity, and produces a complete tokenized semantic projection using only approved tokens such as `<WORKSPACE>` and `<VENV>`. The child independently double-reopens the capsule and installed-file trees. The persisted A/B bindings contain the raw-envelope digests and the complete privacy-safe projection, so every semantic digest can be independently recomputed without raw local paths. A and B must use distinct child processes and match semantically.

The resulting candidate is always unapproved, runtime-inadmissible, execution-inadmissible, and unusable as a formal approval. A later operator-review milestone must perform a fresh formal recapture; this preparation candidate cannot be promoted or replayed as approval.

## D1 and scientific-evidence quarantine

The predecessor's full 550-digest D1/V2 execution-lineage quarantine remains bound by exact count and roster digest. Preparation source/governance custody is not incorrectly rejected merely because the conservative roster intentionally includes governance hashes. Instead, the frozen future scientific-carrier predicate rejects every quarantined digest in a scientific completion/output field and rejects seed 1729 in typed scalar, vector, registry, coordinate, and request-projection seed fields. None of the five preparation events carries scientific completion evidence.

## Nonclaims and next gate

This record does not claim that workspace `src/` was amended, executable preregistration was completed, prerequisite evidence became loadable, the marker or preparation sequence ran, a capsule became execution-admissible, a runtime was approved, a scientific authority ledger was created, or any rank/training/production/scientific/claim/submission state advanced. The 172 unresolved null values and 12 blockers are unchanged.

The next gate is a separate explicit, informed user authorization of the exact irreversible preparation sequence above. Even after such authorization, this package can produce only preparation custody and an unapproved runtime candidate. Runtime review/approval, executable preregistration, blocker closure, a distinct scientific campaign nonce, activation, and all scientific execution remain separate future gates.

## Publication and trust boundary

This internal registration and every future raw v2 custody artifact are excluded from anonymous submission and public release. Raw runtime envelopes are never persisted at all. A publication-safe derivative and a fresh anonymity audit are required; no publication roster or derivative path is frozen here. The human note contains no local absolute path.
