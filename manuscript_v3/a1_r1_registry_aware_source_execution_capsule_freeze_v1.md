# A1 R1 registry-aware overlay-source and coordinate freeze (v1)

## 1. Status and scope

This additive internal preregistration record has the exact state
`R1_A1_REGISTRY_AWARE_OVERLAY_SOURCE_AND_COORDINATE_FREEZE_ZERO_EXECUTION_RUNTIME_ADAPTER_AND_BINDER_DEFERRED_NOT_EXECUTABLE`.
The global project state remains `DRAFT_NOT_EXECUTABLE`.

Despite the historical filename, this milestone freezes only replacement-seed
custody, five virtual one-literal overlays, an explicit static preactivation
source closure, six ordered coordinate manifests, a five-event schedule, and a
descriptive inventory of existing runner APIs. It does not freeze a successor
authority or binder schema and does not claim a materialized or executable
capsule. No source tree, adapter, runtime bundle, registry integration, runner,
metrics, decision, or production-order integration was created.

No entropy, network, subprocess, rank run, model training, confirmatory run,
production run, permit, phase consumption, or scientific result was produced.

## 2. Registry and overlay identity

The frozen registry is
`[4052249444591756, 3253, 5003, 7411, 10007, 13007, 16001, 20011]`, with
semantic digest
`33ac19d64b10571dd8aa53aad6b5219845c6505c23bf1938ff490b471c251780`.
The replacement is ordinal 0; the seven later ordinals are unchanged. Every
entry is a distinct exact non-Boolean integer in `[0, 2^53)`. Exposed seed 1729
remains quarantined across all methods, lanes, and budgets, including uses for
which budget is not applicable.

Five virtual overlays replace only the first seed literal in residual data, the
sampled runner, the exact runner, test-only execution order, and production
order. Each rule binds the named module-level tuple, requires one assignment and
one replacement, preserves every other byte, and makes the resulting tuple
exactly equal to the frozen registry. Base-file identity and virtual-output
identity are recorded separately; none of the five changed virtual outputs is
materialized at its target path.

## 3. Honest source and runtime boundary

The source manifest freezes a lexically ordered 45-module
`PREACTIVATION_MODULE_LOAD_STATIC_CLOSURE`, not a dynamically broadened tree.
All reachable package initializers are parsed and 102 local static-import edges
are frozen. Three additional read-only inputs are bound: `pyproject.toml`, the
reference environment lock, and A1 Specification 62. The full-body source
manifest digest is
`15b04fd7a59ffbb9fdb3cca9abf4e231fce046be3fbc19cee94d29f619f8a4d1`.
It authenticates policy, roots, ordering, all rows and byte identities, static
edges, dynamic-import inventory, the deferred-runtime boundary, registry
semantics, and nonpackage inputs, excluding only its own digest field.

This is not a complete production-execution import graph. The deferred local
runtime chain has six distinct edges: identity dynamically imports approval;
approval normally imports identity, function-locally imports capture, and has a
canonical self import; capture normally imports both identity and approval.
Separately inventoried loader sites record that both the runtime attestor and
capture directly load runtime identity through `spec_from_file_location` and
`exec_module`, while capture can dynamically load external numerical modules.
Approval and capture source bytes are custody-only deferred inputs.
`dynamic_runtime_closure_complete=false` and
`production_execution_import_closure_complete=false`.

Successor runtime identity and approval files, the legacy runtime identity and
approval files, and the legacy runtime-candidate root are all absent under
lstat no-entry checks. The research diagnostic module is only a static audit
implementation; it is not a runtime adapter. Its planned package target remains
absent.

## 4. Coordinate and event freezes

Each tagged coordinate has an enforced phase-local coordinate ordinal derived
from its exact seed ordinal, budget ordinal where applicable, and method
ordinal. Each manifest row separately carries its list ordinal and manifest
domain. Thus COMPLETE_SAMPLED and the aggregate views never repurpose a phase
ordinal. Exact coordinates also bind the legacy two-field `(seed, method)`
projection; sampled coordinates bind `(seed, budget, method)`.

The canonical full-body identities are:

- EXACT, 24: `cddb2f41afac894efc70113a51e273fe228c1f862f5a486eda9f1763f3b17768`
- PRIMARY, 48: `74cba81f4587b55c9112a46da58b94cc73c47a65e2827d6e6ec20e7c53f23f28`
- CONTROLS, 72: `2f55f5561debfb978591a9d4e81f50ab0fee17eac862c45073076dc3865fc460`
- COMPLETE_SAMPLED, 120: `99e4320ee36c298ea3fd410bfe3d0b624df2e501837d3ad99a396eef701d0a50`
- execution-phase schedule, 144: `b9541b1e247d4cf025408063bdcb2462b4c9186b7e6f929efa967c9c98fe2272`
- all-aggregate identity order, 144: `df5b513d80bab4c9ef97c555ea8622396716ec38a7dafb2866298cb2704a73fd`

The two 144-coordinate orders are intentionally different. Execution-phase
order is EXACT, then PRIMARY, then CONTROLS, with a primary-metrics barrier
between the latter two phases. Aggregate identity order is EXACT followed by
COMPLETE_SAMPLED, whose sampled methods are interleaved within each seed and
budget. A separate typed five-event schedule freezes
`RANK -> EXACT -> PRIMARY -> PRIMARY_METRICS -> CONTROLS` under digest
`488e4223c16dbbc54e4f6a438acf629fd5c0c1e8e53f262e53759295afc7cb9c`.

## 5. Descriptive runner API inventory only

The current rank, exact, primary, and control runner, loader, revalidator, and
result symbols are reopened from their frozen source and bound to AST-derived
signature identities without importing project modules. This inventory is
descriptive only. It does not define a request, completion, rank, permit,
consumption, phase-aggregate, or successor-authority schema; it does not qualify
typed coordinate consumption; and it does not establish compatibility with the
legacy launchers. Every launch, execution, and output-admissibility flag is
false.

The existing virtual production-order overlay retains legacy authority,
schema, artifact-root, and runtime constants and therefore cannot mint
successor-admissible records. The immutable
`StaticOverlaySourceFreezeQualification` is available only through the
supported canonical sidecar loader after the sidecar, self digest, all three
registration bindings, and a fresh static audit agree. It proves only the
source-and-coordinate custody snapshot and cannot issue, consume, launch, or
admit an output.

## 6. Preserved preregistration and readiness state

The historical v1 preregistration has 174 unresolved null values: 168
preexecution and 6 deferred postexecution. Closure v2 resolved exactly two
pre-D1 fields, leaving 172: 166 preexecution and 6 deferred. This milestone
closes none of the 12 blockers: 10 confirmatory-execution blockers and 2
claim-promotion/submission blockers remain open. The exact freeze predicate is
still false, all required scientific nonnull conditions remain false, and
`test_data_unopened_before_freeze` remains null.

The current claim ledger, human and machine execution preregistration, CP76
manifest, CP76 readiness test, and CP76 checklist are directly hash-bound and
reopened. The current execution state remains `DRAFT_NOT_EXECUTABLE`; CP76
remains `NOT_READY`, with manuscript submission readiness false. No readiness
transition occurs.

D1/V2 records and checkpoint bytes are custody-only prior development
knowledge. They are not execution inputs, checkpoints, metrics, results,
permits, receipts, or confirmatory evidence. Legacy v1 authorizations, permits,
consumptions, receipts, runtime files, and rglob manifest are also inadmissible.

## 7. Nonclaims and next gate

Binder-schema freeze, successor-authority freeze, typed-coordinate consumption
qualification, phase-aggregate admission qualification, registry integration,
source amendment, overlay materialization, adapter implementation, executable
capsule completion, runtime bundle, binder/runner integration,
production-order activation, primary-metrics integration, and candidate-decision
integration are all false. Rank, training, production, and scientific execution
remain unauthorized. No scientific result is eligible; R1, R2, C17, claim
promotion, and submission readiness remain false.

Before rank or production execution, a separate additive, versioned milestone
must implement and audit a coherent successor authority, runtime, registry-aware
adapter, rank binder, coordinate binder, phase-aggregate admission, runner
integration, and issuance/consumption protocol. That future binder must carry
the execution-phase-schedule manifest, the all-aggregate manifest, and the
five-event schedule digest including the PRIMARY_METRICS barrier. All base and
predecessor bytes bound here must remain unchanged.

## 8. Publication and trust boundary

This registration and all four raw milestone files are internal custody
records, not anonymous-submission or public-release artifacts. Raw predecessor
custody, source paths, and local metadata are excluded. A publication-safe
derivative requires a new path, a separately frozen roster, sanitization, and a
fresh anonymity audit; no derivative path or publication roster is frozen.

These checks are an honest-host procedural control, not a sandbox against an
actor who can mutate process memory, module globals, or registered files. Bound
files are reopened as nonsymlink regular files through nonsymlink directory
ancestors; absence gates use lstat no-entry semantics.
