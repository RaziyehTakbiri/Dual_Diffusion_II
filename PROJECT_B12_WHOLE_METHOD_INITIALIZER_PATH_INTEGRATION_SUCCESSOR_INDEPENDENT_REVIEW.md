# Independent review — B12 whole-method initializer-to-path successor

## Decision

**GO — authorize exactly the existing timetable registration**

`Produce whole-method beta: initializer, continuous path, jump/edit law, and sampler integrated.`

Final finding counts are **P0/P1/P2 = 0/0/0**.

The successor closes the predecessor review's sole P1 implementation-evidence
gap: the exact Test-28 development initializer result now determines a typed
path initial state, and that state is consumed by actual bounded Test-29
lineage/edit and Test-30 addressed-Heun operations.  Primary and separately
authored recomputation outputs are byte-identical.

This GO does **not** authorize `End-to-end method is feature-complete.`  It
does not close B12, any of the 50 real residual predicates, any blocker,
field, Formal Test, result, runtime, data, training, scientific, production,
claim, release, or authority state.  No candidate, tracker, or ledger file was
edited during review.  This independent-review document is the review lane's
only created file.

## Exact reviewed package

| Role | Path | Bytes | Raw SHA-256 |
|---|---|---:|---|
| Human candidate | `PROJECT_B12_WHOLE_METHOD_INITIALIZER_PATH_INTEGRATION_SUCCESSOR.md` | 10,167 | `47e8aefc19513d92937799323907be80b5f77fdc33d44a759a3e56171d86e1b3` |
| Primary source | `src/heterodiff/evaluation/b12_whole_method_initializer_path_integration_successor.py` | 54,992 | `cca5df6d65a4861d019b709cea6abc9ae14fb91a445a1ed4f6738f1ac3b62f84` |
| Separate recomputation | `src/heterodiff/evaluation/b12_whole_method_initializer_path_integration_recomputation.py` | 32,508 | `5c01fb8a195402cf012a815b92bcec13b86845bc088f8b1152d28aa6153a5f5f` |
| Focused hostile tests | `tests/unit/test_b12_whole_method_initializer_path_integration_successor.py` | 34,686 | `ccbb166dd26bfa90c5ec5c0ad0181bee1fbd0f52ec3c6198c955dc32c6a580f6` |
| Isolated hash-first validator | `research/diagnostics/manuscript_v3_b12_whole_method_initializer_path_integration_successor_v1.py` | 46,953 | `4d09ccc6d433d4293b1be68908f83295cb3bd2982ec7c1f473e9280152e8aa21` |
| Canonical machine record | `research/fixtures/manuscript_v3_b12_whole_method_initializer_path_integration_successor_v1.json` | 30,010 | `e247add0ef427cfd2c77f27a9347c28bc0b70df8bb53b803864619d27d1e7ea8` |

All six files were exact regular `0644`, single-link files.  The canonical
machine record is duplicate-key-free, canonical ASCII JSON with terminal LF.
Its independently recomputed self-record is
`384f4a15fa28c18f3e84177f7bc42332b612ac8ed3afff6a5a5ee918b02ab107`.

The exact stable receipt is
`7f3af61499f4c618daa38d72e38570c4759c5e146eeeef61bb182b9b4f20e102`.
The primary and independent core SHA-256 is
`73887c5411e8822942c9c37ddbdfb1a485f96ef1a2fce4c4ff56f503b4b9bc8e`.
The isolated 57-member capsule-manifest SHA-256 is
`2417df50511157a18f5d6a4a327147f3cc61f3bb395ba17ad969d719ffd9fb88`.

## Independent reproduction

The review used Python 3.11.5 and pytest 9.1.1.  The exact isolated validator
passed from the project root and from an unrelated physical copy of the
candidate and dependency tree:

```text
PASS_B12_WHOLE_METHOD_INITIALIZER_PATH_SUCCESSOR_PENDING_INDEPENDENT_REVIEW — record 384f4a15fa28c18f3e84177f7bc42332b612ac8ed3afff6a5a5ee918b02ab107; receipt 7f3af61499f4c618daa38d72e38570c4759c5e146eeeef61bb182b9b4f20e102; core 73887c5411e8822942c9c37ddbdfb1a485f96ef1a2fce4c4ff56f503b4b9bc8e
```

Exact test results were:

- focused successor hostile suite: **41/41 passed**;
- bounded adapter, integration, Test-29/path, CKS, and training-freeze
  compatibility roster: **234/234 passed**;
- total distinct focused plus compatibility tests: **275/275 passed**.

The exact compatibility roster was the external author-extension components,
two-domain adapter stack, B12 integration stack, finite-acyclic Test-29 oracle,
two-macrostep Test-29/Test-30 path, two-domain count-normalized production CKS
projection, and F139--F144/F147 training/checkpoint freeze suites.

Two additional fresh `-I -S -B` processes executed the primary and independent
implementations separately.  Each emitted the same 9,058-byte core with SHA
`73887c5411e8822942c9c37ddbdfb1a485f96ef1a2fce4c4ff56f503b4b9bc8e`.

## Closed predecessor finding: real initializer-to-path data flow

The frozen initializer executes the accepted CP62 request row, exact rational
score provider, certified facade, and v2 fixed-budget SIR kernel.  Its owner
validates the live result before transformation.  Runtime-local result identity
can differ between allocations, while the stable semantic projection remains
equal at
`5bca3f822a6a526fb0775cc7bf422347df7e65227141b4f2c76a462d3d597f85`.

The actual selected configuration is the exact empty tuple with digest
`c9450132be2800eddc7e8e36547c49e8b7839e1e282e32f0736b453267b92b06`.
The typed transform validates the concrete event type, dimension, coordinate,
ordinal, and digest roster.  It produces the empty typed initial state with
digest
`2338839b5c7df9c0845063a4053e6ab40d16132f232713ef515d5599d728f05f`
and transform digest
`72a27a8f315e4a1fa95933fde4fe8711d08bcd1d00766dea80bf50275ebcb5b4`.

That state, selected-configuration digest, transform digest, bounded policy,
and words `(2, 27)` determine path-input digest
`63aa54613cad1b89973bbceb83cab2479f2cacbcd92e8c2b925f1ff34912f9a4`.
The path begins from that exact state.  Step zero runs an accepted
zero-cardinality Test-29 birth route, creates serial 1, and subjects the new
occurrence to an actual CP23-addressed right Test-30 Heun update.  Step one
subjects the same serial to an addressed left Heun update and then applies the
accepted Test-29 death route.  CP24 central-word custody, Test-29 selection and
lineage advance, CP23 increment rosters, serial continuity, and left/jump/right
state digests are all explicit.  The final path-report digest is
`15b46792946d98a9893ba3b7fe31ff83c483500c45751564c8bfbbe9fb247b81`.

The suite also executes a valid nonempty selected configuration and proves its
exact selected coordinate changes the first Heun arithmetic and path report.
Substituting or omitting the selected configuration, transform, typed state,
source-event association, or path custody either changes the bound digest or
fails closed.  Thus the actual empty selected state is not merely a decorative
sibling summary.

## Independent digest and source checks

Independent canonical recomputation matched all seven nested custody layers:

- initial-state SHA-256 `2338839b5c7df9c0845063a4053e6ab40d16132f232713ef515d5599d728f05f`;
- transform SHA-256 `72a27a8f315e4a1fa95933fde4fe8711d08bcd1d00766dea80bf50275ebcb5b4`;
- path-input SHA-256 `63aa54613cad1b89973bbceb83cab2479f2cacbcd92e8c2b925f1ff34912f9a4`;
- path-report SHA-256 `15b46792946d98a9893ba3b7fe31ff83c483500c45751564c8bfbbe9fb247b81`;
- end-to-end custody SHA-256 `037d50b89289979c8b40bc843f14fd47fc0365792c0b12c4315b4132c6e428ca`;
- core SHA-256 `73887c5411e8822942c9c37ddbdfb1a485f96ef1a2fce4c4ff56f503b4b9bc8e`;
- receipt SHA-256 `7f3af61499f4c618daa38d72e38570c4759c5e146eeeef61bb182b9b4f20e102`.

A conservative recursive AST import walk from the primary and independent
sources found exactly 56 local source files.  The validator manifest contains
all 56 exactly once plus the sealed predecessor machine record, with no
missing or extra member.  The primary dynamically compiles only the captured,
hash-pinned independent source.  The independent source neither imports nor
calls the primary.  Neither implementation imports or executes the legacy
whole-method runner or its separately frozen Test-30 initial state; the
predecessor is consumed only as exact hash-first machine evidence.

## Trust-boundary and hostile review

The ordinary public API truthfully remains
`direct_public_api_custody_authenticated=false`.  Authoritative qualification
requires the isolated validator.  It performs a descriptor-relative
`O_DIRECTORY|O_NOFOLLOW` parent walk and `O_NOFOLLOW` leaf capture, requires
stable regular `0644` single-link files, copies captured bytes into a fresh
private `0700` capsule, and revalidates workspace and capsule identities after
execution.

The child is a fresh `-I -S -B` interpreter with a fixed environment and an
unrelated empty working directory.  It creates the declared minimal package
namespaces, loads only manifested local sources, rejects cold CP62 calibration
launches and unexpected local modules, validates exact primary/independent
parity, and emits bounded canonical JSON.  The parent rejects nonzero exit,
timeout, stderr, duplicate keys, malformed framing, roster drift, and exact
semantic drift.

Hostile tests and independent inspection cover:

- root, intermediate-parent, and leaf symlinks;
- hardlinks, wrong mode, same-size byte substitution, unstable file and root
  identities, workspace replacement, and capsule tampering before or after
  child execution;
- preloaded module and package-attribute cache poison, including an explicit
  `None` entry, with exact parent-state restoration;
- `PYTHONPATH`, `sitecustomize`, current-directory source shadows, and stale
  bytecode;
- concurrent isolated validators producing identical receipts;
- child failure, stderr, malformed output, duplicate JSON keys, and bounded
  output enforcement;
- exact-type, unsupported-event, dimension, cardinality, word, digest,
  lineage, address, increment, state, receipt, and custody substitutions; and
- attempts to reach the predecessor runner or the old frozen Test-30 initial
  occurrences.

The capsule truthfully records that package initializer files are manifested
but not executed under policy `MINIMAL_NAMESPACE_NO_INIT_EXECUTION`, and that
NumPy/SciPy site-package files are external to the private source capsule.
Those runtime limitations do not defeat this exact code-only,
nonconfirmatory beta milestone because runtime/production qualification is
explicitly excluded, the interpreter and dependency versions are checked,
and the reconstructed output is exact.  They cannot be reused as evidence for
Gate B0, production runtime, or scientific execution.

## Exact authorization and unchanged state

This review authorizes a later tracker registration of exactly one existing
task:

- **eligible:** `Produce whole-method beta: initializer, continuous path,
  jump/edit law, and sampler integrated.`

It explicitly does not authorize:

- **ineligible:** `End-to-end method is feature-complete.`

The latter still requires a general arbitrary-length method surface and the
remaining operational integrations; this successor truthfully supplies only
the bounded two-macrostep beta.  Formal Tests 28 and 29 remain `OPEN`; Formal
Test 30 remains `PENDING`.  All 50 real residual receipts remain absent.

Exact review deltas are therefore:

- review evidence for the one beta task: **GO**;
- author-applied timetable checkbox delta: **0**;
- tracker and ledger edits: **none**;
- field delta: **0**;
- blocker delta: **0**;
- Formal-Test delta: **0**;
- result and B12 delta: **0**;
- data, runtime, training, science, production, claim, release, and authority
  deltas: **0**.
