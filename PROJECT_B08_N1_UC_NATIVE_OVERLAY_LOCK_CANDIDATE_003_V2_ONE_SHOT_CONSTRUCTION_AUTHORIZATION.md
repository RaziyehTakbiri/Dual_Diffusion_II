# B08 N1 UC-native candidate-003 V2 one-shot construction authorization

**Issued:** 2026-09-03  
**Authorization state:** exact reviewed package eligible for one operator-activated attempt  
**Candidate:** `b08-n1-overlay-candidate-003`  
**Attempt budget:** one; retry, repair, replacement, and reuse prohibited  
**Scientific/data authority:** none  
**Tracked project delta at issuance:** zero

## 1. Authority basis

The exact [V2 default-off Databricks preflight outcome](PROJECT_B08_N1_UC_NATIVE_OVERLAY_LOCK_CANDIDATE_003_V2_DATABRICKS_PREFLIGHT_OUTCOME.md)
and its [independent hostile review](PROJECT_B08_N1_UC_NATIVE_OVERLAY_LOCK_CANDIDATE_003_V2_DATABRICKS_PREFLIGHT_INDEPENDENT_REVIEW.md)
establish an error-free preflight with:

- exact reviewed builder and canonical launcher identities;
- exact 304-file reviewed selected-source snapshot;
- review-package record SHA-256
  `5404dd580fac351e888d40836a399d5490396f35df576013eb1a78ecd20d9b23`;
- exact wheel-selection ABI runtime and deterministic environment;
- valid V2 native profile;
- candidate-003 virtual prefix and all 132 reserved leaves absent;
- canonical F152 lock absent; and
- no prior candidate-003 write, network, resolution, build, install, data, Spark,
  REST, or scientific operation.

That review found P0/P1/P2 `0/0/0` and reproduced the package digest
independently. Candidate-003 was absent and unspent as observed. This record
accepts that exact package for one bounded construction attempt; it does not
claim raw chat-transport bytes, authenticated operator identity, attested
execution time, or future state stability.

## 2. Exact package-bound authorization

The independently reviewed package authorization token is exactly the following
112-character value, with no spaces, brackets, or line breaks:

```text
AUTHORIZE_REVIEWED_CANDIDATE_003_PACKAGE_SHA256_5404dd580fac351e888d40836a399d5490396f35df576013eb1a78ecd20d9b23
```

This token is valid only while all objects and observations bound by the V2
review package remain exact. Any source, launcher, builder, snapshot, native
profile, target/probe review, probe outcome, environment, runtime ABI,
destination, or reserved-leaf change invalidates this authorization before use.
No substitute digest, prefix, candidate number, or path is permitted.

## 3. Exact operator inputs

Use only the reviewed hash-first launcher. The five widget values must be:

```text
b08_n1_uc_native_launcher_expected_builder_sha256
7c7edb28f459618b1f35538e444b9cf40e70026b02fd0919ded9a20097f9014d

b08_n1_uc_native_execution_mode
CONSTRUCT_ONE_UC_NATIVE_REVIEW_PENDING_CANDIDATE_003

b08_n1_uc_native_network_build_authorized
true

b08_n1_uc_native_one_shot_acknowledgement
AUTHORIZE_ONE_DATA_FREE_N1_UC_NATIVE_NETWORK_BUILD_CANDIDATE_003

b08_n1_uc_native_review_package_authorization
AUTHORIZE_REVIEWED_CANDIDATE_003_PACKAGE_SHA256_5404dd580fac351e888d40836a399d5490396f35df576013eb1a78ecd20d9b23
```

The UI boolean is lowercase `true`. The package token is entered last. Setting
these values is the operator's explicit activation of network/build authority;
this document does not claim that activation has already occurred.

## 4. One-shot operating order

1. Make no change to the builder, launcher, selected 304 source files, snapshot,
   native profile, target/probe reviews, or probe outcome.
2. Use the same dedicated DBR 17.3 x86_64 CPU cluster and retain all 15 exact
   deterministic environment variables.
3. Do not attach, open, or reference study or test data.
4. Confirm the canonical F152 lock and every candidate-003 reserved leaf remain
   absent; do not create the candidate path manually.
5. Open only
   `databricks/notebooks/b08_n1_uc_native_overlay_lock_candidate_launcher.py`.
6. Retain the exact builder-hash widget, enter the construction mode,
   acknowledgement, and lowercase network/build boolean, then paste the
   review-package token last.
7. Select **Run all exactly once**. Do not run the builder directly and do not
   start a second or concurrent launcher run.
8. Return the complete final JSON. Do not install, publish, copy, move, repair,
   delete, or reuse any candidate object while independent review is pending.

Once the authorized run passes its repeated preflight, construction begins
immediately; there is no second pause.

## 5. Spent-at-intent and terminal handling

The first candidate intent exclusive-create call makes candidate-003 spent or
possibly spent. After that boundary, any failure, interruption, cluster detach,
ambiguous notebook output, visible reserved leaf, or terminal receipt consumes
the only attempt.

A failure proved to occur before intent may leave the candidate namespace
unspent, but it still exhausts this authorization. A new run would require a
fresh review and separately issued authority; it must never be inferred from
namespace absence alone.

In every such case:

- do not rerun candidate-003;
- do not delete, rename, replace, repair, or complete its objects;
- do not reuse its prefix or any reserved leaf; and
- return the complete available JSON or error output for independent review.

The expected successful terminal decision is:

```text
CANDIDATE_CONSTRUCTED_REVIEW_REQUIRED_DO_NOT_INSTALL
```

Even that decision is review-pending. It is not permission to install the
overlay or publish the canonical F152 lock.

## 6. Authorized and excluded effects

This one-shot activation authorizes only the builder's bounded, data-free
dependency resolution, wheel download/build, isolated overlay construction,
verification, and append-only candidate-003 evidence publication against its
fixed index configuration. Third-party build tools are not OS-sandboxed, and
future index availability, Databricks egress, transitive resolution, cluster
continuity, large managed-storage writes, and whole-runtime satisfaction remain
residual risks.

It does not authorize:

- study or test data access;
- calibration, training, inference, or Formal Tests;
- installation into or mutation of the Databricks base runtime;
- installation or activation of the candidate overlay;
- canonical F152 lock publication;
- a retry or successor candidate;
- deletion, repair, replacement, or reuse of evidence;
- B08/Wave-2, field, blocker, result, or timetable closure;
- scientific claim promotion; or
- release or submission.

## 7. Project-state effect

Issuing this exact one-shot package authorization is a non-counted readiness
checkpoint. Until a successful construction output is returned and
independently accepted, the project state remains:

- Marked tasks: **62 checked / 101 open / 163 total**
- Fields: **24 open / 148 closed**
- Blockers: **7 OPEN / 5 CLOSED**
- Formal Tests 28/29/30: **OPEN / OPEN / PENDING**
- Result slots: **0/4**
- F151/F152: **OPEN and null**
- B08 and Wave 2: **OPEN**

This record changes no tracked task or scientific state.
