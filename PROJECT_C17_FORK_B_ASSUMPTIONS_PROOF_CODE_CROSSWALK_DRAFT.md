# C17 Fork-B assumptions and proof/code crosswalk draft

**Package state:** `C17_FORK_B_ASSUMPTIONS_AND_PROOF_CODE_CROSSWALK_DRAFT_VALIDATED_UNPROVED`  
**Global project state:** `DRAFT_NOT_EXECUTABLE`  
**Selected route:** `FORK_B_DIRECT_SIMULTANEOUS_TARGET_OCCUPATION_CERTIFICATES`  
**Theorem status:** `UNPROVED`  
**Scientific effect:** zero

## 1. Authority and scope

The normalized visible authority for this additive local package is exactly:

> Sounds great. Go ahead and finish them in parallel. Mark all the completed tasks as the end.

The normalized UTF-8 text is 92 bytes and has SHA-256
`465aa47a0714b7914e33b6b6772afbfad3a56959cb6eb9f10b8e98f39c0f8d38`.
Raw transport bytes, trailing HTML-space transport content, the conversation
envelope, account identity, and a cryptographic user signature are not bound.
The sentence authorizes local additive Block-2/Block-3 draft construction,
read-only qualification, and later one-way tracker marking after independent
audit. It does not authorize external contact, browsing, data access, runtime
approval, training, a scientific run, a formal-test result, claim promotion,
submission, or modification of an existing evidence package.

The user did not select the four filenames, schema, theorem bookkeeping, or
the exact route values in this package. Those are agent-selected bounded
implementation details constrained by the already frozen route-selection
record. The four additive paths are:

1. `PROJECT_C17_FORK_B_ASSUMPTIONS_PROOF_CODE_CROSSWALK_DRAFT.md`;
2. `research/fixtures/manuscript_v3_c17_fork_b_assumptions_proof_code_crosswalk_draft_v1.json`;
3. `research/diagnostics/manuscript_v3_c17_fork_b_assumptions_proof_code_crosswalk_draft_v1.py`; and
4. `tests/unit/test_manuscript_v3_c17_fork_b_assumptions_proof_code_crosswalk_draft_v1.py`.

No tracker is edited by this package. A later tracker may consume the validated
project-control predicate one way, but must not convert it into a theorem,
field, blocker, formal-test, execution, or result closure.

## 2. Exact route disposition

The predecessor freeze selected target-first
`KL(P_H || P_HHAT)`, component order
`K0, KC, K_PLUS, K_MINUS, K_REPLACEMENT`, and corresponding simultaneous
upper bounds `U0, UC, U_PLUS, U_MINUS, U_REPLACEMENT`. The selected
common-support policy is
`ACQUISITION_JUSTIFIED_POSITIVE_DOMINATED_MIXTURE_WITH_SHARED_BASE_STRUCTURAL_ZEROS_AND_FAIL_CLOSED_NONADMISSION`.

This package classifies Fork B as
`CONDITIONALLY_VIABLE_PROOF_PROGRAM_NOT_CURRENTLY_DISCHARGED_OR_NONVACUOUS`.
That means only that the requested certificates would imply the stated upper
bound after the path-space identity and every assumption have independently
been proved. It does **not** mean that any certificate exists, that the exact
target occupation law is available, that a dominating measure is admitted,
or that either real domain satisfies common support.

The target identity remains

```text
KL(P_H || P_HHAT)
  = K0 + KC + K_PLUS + K_MINUS + K_REPLACEMENT,
```

and the Fork-B conclusion remains conditional:

```text
if C17's identity is proved and one simultaneous valid event establishes
K0<=U0, KC<=UC, K_PLUS<=U_PLUS, K_MINUS<=U_MINUS,
K_REPLACEMENT<=U_REPLACEMENT, then
KL(P_H || P_HHAT) <= U0+UC+U_PLUS+U_MINUS+U_REPLACEMENT.
```

The logical implication is bookkeeping, not a proof or numerical
certificate. NCE value evidence is not substituted for derivative or legal
edge error. A global architecture ceiling is not an error relative to the
unknown exact residual. Cap/reference defects are not added as a sixth KL
term.

## 3. Exact A1--A12 assumption inventory

Every row is required for the current theorem target. Every general-theorem
and real-domain verification flag remains false.

| ID | Required assumption | Current disposition | Why it remains open |
|---|---|---|---|
| A1 | Capped quotient state is standard Borel; active strata have declared charts; boundary/local-time terms cancel or are explicit | `OPEN_PARTIAL_CODE_SURFACE` | Typed capped objects do not prove quotient measurability or boundary cancellation |
| A2 | Candidate-base martingale problem is well posed and nonexplosive with the exact disjoint jump-family kernel | `OPEN_PARTIAL_CODE_SURFACE` | Reference simulators do not prove the general martingale problem or full unlabeled-kernel identity |
| A3 | Exact, guide, and plug-in information functions are measurable, positive, finite, and have positive finite normalizers on occupiable states | `OPEN_POLICY_SELECTED_DOMAIN_UNVERIFIED` | The common-support policy is selected, but no domain is admitted and exact information functions are unavailable |
| A4 | Exact and plug-in terminal densities agree and both controlled generators vanish on the clean hold | `OPEN_SPECIFICATION_ONLY` | Frozen boundary requirements are not a proof for the general laws |
| A5 | Within-stratum log gradients exist predictably and their target-occupation quadratic form is finite | `OPEN_PARTIAL_CODE_SURFACE` | Gradient interfaces do not prove regularity or target-occupation integrability |
| A6 | Both laws share covariance and the drift difference lies in the Cameron--Martin range, including degenerate directions | `OPEN_SPECIFICATION_ONLY` | No general controlled-law range/Girsanov argument is present |
| A7 | Every legal jump family has common support, exact RN/log-ratio factors, structural-zero agreement, and finite occupied-edge Bregman terms | `OPEN_PARTIAL_CODE_SURFACE` | Edit interfaces and finite diagnostics do not establish the general occupied-edge law |
| A8 | Controlled exit rates and all three jump-family compensator integrals are predictable and finite under the target law | `OPEN_FINITE_FIXTURES_ONLY` | Finite diagnostics are not a general or real-domain integrability proof |
| A9 | Initial, diffusion, and marked-jump likelihood ratios form a true uniformly integrable martingale | `OPEN_NO_UI_PROOF` | No Novikov/Kazamaki/jump criterion or justified localization limit is supplied |
| A10 | The exact and plug-in controlled martingale problems are unique in law | `OPEN_NO_UNIQUENESS_PROOF` | No general controlled-law uniqueness theorem is supplied |
| A11 | The comparison is between ideal continuous-time laws, with numerical and operational-surrogate errors separated | `OPEN_INTERFACE_SEPARATION_ONLY` | The separation is specified, but the ideal learned law is not implemented or proved |
| A12 | The target conditioning is only relative to the frozen candidate base; broader data-forward reversal requires separate base and initial-law proofs | `OPEN_SCOPE_DECLARED_BROADER_EQUALITY_UNPROVED` | The required base-learning and reverse-initial-law identification are absent |

The package closes none of A1--A12. “Partial code surface” means that a named
interface can support a future proof obligation; it is never evidence that
the mathematical assumption is true.

## 4. Proof-obligation register

The exact ordered register is:

1. `PO01_QUOTIENT_STATE_MEASURABILITY_AND_BOUNDARY_TERMS`;
2. `PO02_BASE_MARTINGALE_PROBLEM_WELLPOSEDNESS_AND_NONEXPLOSION`;
3. `PO03_POSITIVE_FINITE_INFORMATION_COMMON_SUPPORT_AND_NORMALIZERS`;
4. `PO04_TERMINAL_AND_CLEAN_HOLD_COMPATIBILITY`;
5. `PO05_WITHIN_STRATUM_REGULARITY_AND_TARGET_INTEGRABILITY`;
6. `PO06_SHARED_DIFFUSION_RANGE_CONDITION_AND_GIRSANOV`;
7. `PO07_JUMP_SUPPORT_RADON_NIKODYM_FACTORS_AND_STRUCTURAL_ZEROS`;
8. `PO08_FINITE_TARGET_COMPENSATORS`;
9. `PO09_TRUE_UI_LIKELIHOOD_MARTINGALE_OR_JUSTIFIED_LOCALIZATION`;
10. `PO10_CONTROLLED_PATH_LAW_UNIQUENESS`;
11. `PO11_IDEAL_LAW_VERSUS_NUMERICAL_AND_OPERATIONAL_ERROR_SEPARATION`;
12. `PO12_CANDIDATE_BASE_CONDITIONING_SCOPE`;
13. `PO13_INITIALIZER_KL_DERIVATION_AND_ORIENTATION`;
14. `PO14_FIVE_TERM_PATH_LIKELIHOOD_DECOMPOSITION`;
15. `PO15_SIMULTANEOUS_TARGET_OCCUPATION_DIRECT_CERTIFICATES`;
16. `PO16_CAP_REFERENCE_AND_RESIDUAL_NO_DOUBLE_COUNTING`;
17. `PO17_FINITE_AND_MIXED_KNOWN_LAW_INSTANTIATION_AND_FALSIFICATION`;
18. `PO18_PROOF_CODE_AUDIT_AND_NONVACUITY_THRESHOLD`.

Every obligation is `OPEN`; none is discharged for the general theorem,
PhysioNet, or Retail. A finite fixture may falsify an identity or exercise a
component, but cannot establish `PO17` or promote C17 without the complete
mixed and real-domain packet.

## 5. Direct-certificate contract

Each certificate row must eventually bind the exact mathematical quantity,
target occupation law or admitted dominating measure, exact RN factors when
dominated, estimator/proposal, normalization, family partition, finite bound,
simultaneous uncertainty rule, failure disposition, and a nonvacuity
threshold. The required mapping is:

| Quantity | Bound | Required measure | Current state |
|---|---|---|---|
| Initializer `K0` | `U0` | exact target initial law | `OPEN_NULL_BOUND` |
| Continuous gradient `KC` | `UC` | exact target occupation or proved domination | `OPEN_NULL_BOUND` |
| Birth `K_PLUS` | `U_PLUS` | exact target birth compensator or proved domination | `OPEN_NULL_BOUND` |
| Death `K_MINUS` | `U_MINUS` | exact target death compensator or proved domination | `OPEN_NULL_BOUND` |
| Replacement `K_REPLACEMENT` | `U_REPLACEMENT` | exact target replacement compensator or proved domination | `OPEN_NULL_BOUND` |

All five bound values, certificate paths, occupation receipts, dominating
measure receipts, RN-factor receipts, simultaneous-event receipt, and
nonvacuity decision are typed null in this draft. Missing or invalid support,
an uncovered legal edge family, a nonfinite bound, a vacuous bound, or an
invalid simultaneous rule must fail closed without substituting an NCE loss.

## 6. Proof/code crosswalk at the historical snapshot

The validator records exact hashes for this snapshot and separately checks
AST symbol presence. These are historical inputs, not immutable reverse
dependencies: normal package validation does not require future source bytes
to remain unchanged. A separate freeze-audit function reproduces this exact
snapshot. Future source materialization or refactoring requires a new
crosswalk audit, not mutation of this package.

| C17 object | Historical live symbol | Strength and limitation |
|---|---|---|
| Capped reference state | `CappedPoissonConfigurationReference` | Partial representation only; no target tilt or path certificate |
| Base reference characteristics | `ReversibleHybridReference` | Partial reference process only; no learned controlled-law proof |
| Terminal/association law | `evaluate_association_observation`, `association_log_density_coordinate_gradients` | Admitted-family value/gradient interface; no real-domain admission or intermediate exact information |
| Analytic guide | `AnalyticAssociationPreconditioner.evaluate`, `.coordinate_gradients`, `.edit_log_ratio`, `.cap_boundary_defect`, `.estimate_cap_boundary_from_proposal` | Guide and diagnostics; no exact residual or additive sixth KL term |
| Learned residual interface | `configuration_residual`, `configuration_residual_state_pair_difference`, `configuration_residual_coordinate_gradients` | Value/difference/gradient interface only; no certified checkpoint or error-to-exact-residual certificate |
| Finite joint/product risk | `equal_prior_logistic_risk_per_time`, `population_equal_prior_logistic_risk` | Finite population oracle only; no derivative/edge bridge |
| Finite initializer and jump path KL | `conditional_initial_law`, `tilted_path_kl` | Jump-only finite gate; no continuous term or general quotient-state proof |
| Finite CTMC path KL | `ctmc_path_kl` | Classical finite diagnostic; binary numerical evaluation is not a rigorous general certificate |
| Finite association bridge | `FiniteAtomicAssociationBridgeOracle` | A1-scale oracle; not representative of mixed or real domains |
| Factorized mixed known-law fixture | `build_mixed_ctmc_ou_known_law_oracle` | Partial finite-factorized fixture; no association marginalization, learned residual, cap cancellation, or general C17 |
| Finite Fork-B association diagnostic | historical module `finite_association_fork_b_diagnostic.py` | Partial diagnostic only; no simultaneous general theorem certificate |
| Mixed path-KL diagnostic | historical module `mixed_ctmc_ou_path_kl_diagnostic.py` | Finite factorized numerical check only |
| Cap-defect diagnostic | historical module `mixed_ctmc_ou_cap_defect_cancellation_diagnostic.py` | Algebraic cancellation diagnostic; no complete stability route |
| Totalized operational composer | `configuration_totalized_jump_potential_composer_torch.py` | Operational surrogate; explicitly not analytic `h`/`hhat` |
| Reference-candidate preflight | `ProcessValidReferenceJumpComposer` | Candidate construction only; no ideal controlled clock or complete sampler |

Four historically named sources are absent at the recorded snapshot:

- `src/heterodiff/theory/mixed_hybrid_oracle.py`;
- `src/heterodiff/theory/mixed_hybrid_conditional_oracle.py`;
- `src/heterodiff/theory/mixed_hybrid_conditional_sampler.py`; and
- `src/heterodiff/models/configuration_potential_composer_torch.py`.

The executable-method specification names some of them. The exact disposition
is `METHOD_SPEC_LIVE_SOURCE_CROSSWALK_CONFLICT_UNRESOLVED`. Their historical
description cannot act as current importable evidence. Their absence is a
snapshot observation, not a permanent absence gate: legitimate future source
may be added, after which a new one-way audit must bind its actual bytes,
semantics, and tests.

## 7. Exact unresolved gaps and impossibility boundary

The currently impossible selections are finite general or real-domain values
for `h`, `r*`, target occupation, dominating RN factors, and the five
simultaneous bounds. No local static document can invent them. The following
remain absent or unproved:

- a general same-context joint/product trainer and nuisance branch;
- exact unknown residual `r*` or error-to-`r*` derivative/edge certificates;
- target occupation or a proved admitted dominating law with exact RN factors;
- a UI hybrid change-of-measure proof and controlled-law uniqueness;
- a complete ideal mixed path law with all legal jump families;
- a nonoverlapping cap/reference stability route;
- a rigorous simultaneous finite-certificate construction;
- mixed known-law coverage sufficient for `PO17`; and
- any admitted, nonvacuous PhysioNet or Retail instantiation.

A distribution-free value-risk-to-gradient-and-edge inverse inequality on the
unrestricted declared class is not available. Fork B avoids making that
coercivity claim, but it does not make the hard target-occupation quantities
observable.

## 8. Field, blocker, and claim effects

This draft maps B01 and F001--F006 without closing them:

| Item | Status after this package | Value |
|---|---|---|
| B01 `final-c17-statement-and-claim-wording` | `OPEN` | not closed |
| F001 final theorem statement | `OPEN` | null |
| F002 assumption inventory | `OPEN` | null; this is a draft project-control inventory only |
| F003 proof artifact path | `OPEN` | null |
| F004 code-definition crosswalk path | `OPEN` | null; this package is a draft audit, not the final crosswalk |
| F005 excess-risk-to-hybrid-Dirichlet control statement | `OPEN` | null; Fork B does not select such a coercivity statement |
| F006 coercivity or identifiability assumptions | `OPEN` | null |

The package closes 0 of 172 unresolved fields, 0 of 12 blockers, 0 formal
tests, and 0 scientific results. It does not alter C17, C19, the selected CKS
route, common-support admission, a manuscript claim, or an execution gate.
The sole eligible project-control effect after exact validation is
`C17_FORK_B_ASSUMPTIONS_AND_PROOF_CODE_CROSSWALK_DRAFT_VALIDATED=true`.

## 9. Qualification and allowed conclusion

The read-only validator checks the immutable preregistration/closure and the
entire selected-route quartet, then checks this package's exact canonical
machine record and package bindings. The hostile suite uses only temporary
replicas. It checks exact types, rosters, hashes, assumptions, obligations,
certificate nulls, source-symbol snapshot claims, historical absence
semantics, route identity, common support, nonclaims, and source safety. It
does not import project science or execute a theorem diagnostic.

The only permitted conclusion is:

> Fork B is a coherent conditional proof program, but C17 remains unproved.
> A1--A12, PO01--PO18, U0/UC/U_PLUS/U_MINUS/U_REPLACEMENT, B01, and
> F001--F006 all remain open. No manuscript claim, real-domain admission,
> runtime approval, data access, or scientific execution is authorized.

