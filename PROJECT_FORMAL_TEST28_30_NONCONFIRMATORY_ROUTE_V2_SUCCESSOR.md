# Formal Tests 28–30 nonconfirmatory route V2 successor

## Candidate disposition

This additive V2 package proposes closure of exactly one existing timetable
task, pending independent review:

> Required Tests 28–30 routes run end to end on nonconfirmatory/synthetic inputs.

It does not close Formal Test 28, 29, or 30.  Their states remain respectively
`OPEN`, `OPEN`, and `PENDING`.  It does not close B12, a field, blocker, result,
runtime, production predicate, data predicate, scientific predicate, claim, or
authority predicate.  It edits neither the project timetable nor evidence
ledger and applies no checkbox itself.

The rejected V1 route candidate and its NO-GO review remain immutable historical
evidence.  This V2 package is a successor; it modifies no V1 route byte and no
whole-method predecessor byte.

## Three exact route components

The V2 receipt jointly binds these three disjoint components:

1. The accepted CP63 Test-28 historical rehearsal remains exactly 16 request
   rows, two repetitions per row, 32 launches, and an independently reconstructed
   554-estimand inventory.  Its accepted receipt is revalidated from exact pinned
   fixture and implementation bytes.  V2 truthfully records
   `fresh_execution_performed_here=false` and `confirmatory_evidence=false`.
2. The accepted hash-first Test-29/Test-30 parent-custody wrapper executes afresh
   over all 1,024 ordered word-pair cases.  All 1,024 input and report digests are
   distinct, all four parent sources are hash-bound, and the wrapper is compiled
   only from captured bytes.  This remains a bounded synthetic two-macrostep
   route, not a general path or a Formal-Test result.
3. The independently accepted repaired whole-method initializer-to-path successor
   is bound by exact machine bytes, machine self-record, supplied-input digest,
   primary/independent core digest, stable receipt, and independent GO review.
   The selected Test-28 configuration is cryptographically connected through
   the transform and typed initial state to the integrated Test-29/Test-30 path.

## Repaired whole-method binding

The exact predecessor bindings are:

- machine: 30,010 bytes, SHA-256
  `e247add0ef427cfd2c77f27a9347c28bc0b70df8bb53b803864619d27d1e7ea8`;
- machine self-record:
  `384f4a15fa28c18f3e84177f7bc42332b612ac8ed3afff6a5a5ee918b02ab107`;
- stable whole-method receipt:
  `7f3af61499f4c618daa38d72e38570c4759c5e146eeeef61bb182b9b4f20e102`;
- primary and independent core:
  `73887c5411e8822942c9c37ddbdfb1a485f96ef1a2fce4c4ff56f503b4b9bc8e`;
- independent GO review: 11,076 bytes, SHA-256
  `e24f2e97a67048323170b44d6e537ab07c3a7a6692cf682bc3053c1165732765`.

V2 does not merely accept the reconstructed chain digests by format.  It
independently recomputes the canonical machine self-record, supplied-input
digest, canonical core digest, whole receipt domain digest, selected-state
digest, transform digest, integrated path-input digest, path-report digest, and
end-to-end custody digest.  The stable initializer particle/ESS/stream
projection is not present in the machine core and is therefore not reconstructed
here; V2 requires and transitively binds its exact independently accepted digest
through the fixed machine, core, receipt, custody, and GO-review evidence.  It
then requires the exact accepted values at every layer:

- supplied input:
  `f7e213442d073f88df73d2b33c21e43add4269a8a45b07714bfbd60b4b4ff971`;
- stable initializer execution:
  `5bca3f822a6a526fb0775cc7bf422347df7e65227141b4f2c76a462d3d597f85`;
- selected configuration:
  `c9450132be2800eddc7e8e36547c49e8b7839e1e282e32f0736b453267b92b06`;
- transform:
  `72a27a8f315e4a1fa95933fde4fe8711d08bcd1d00766dea80bf50275ebcb5b4`;
- derived initial state:
  `2338839b5c7df9c0845063a4053e6ab40d16132f232713ef515d5599d728f05f`;
- integrated path input:
  `63aa54613cad1b89973bbceb83cab2479f2cacbcd92e8c2b925f1ff34912f9a4`;
- integrated path report:
  `15b46792946d98a9893ba3b7fe31ff83c483500c45751564c8bfbbe9fb247b81`;
- custody chain:
  `037d50b89289979c8b40bc843f14fd47fc0365792c0b12c4315b4132c6e428ca`.

The route requires positive structural flags for local Test-28 initializer
admissibility, initializer-to-path integration, the bounded two-macrostep path,
Test-29 route/lineage semantics, Test-30 Heun primitives, isolated hash-first
validation, independent GO review, and equal separate recomputation bytes.  It
simultaneously requires the production-law, general-path, Gate-B0 completeness,
runtime, external-runtime, production, confirmatory, and closure flags to remain
false.  All 50 real residual slots remain open.

## V2 subject and receipt integrity

The route creates a new V2 subject digest over the complete CP63 component,
complete fresh Test-29/Test-30 component, exact component IDs, and complete
whole-method successor binding.  The outer V2 receipt then binds that subject
and every state, effect, nonclaim, and proposed-task field under a distinct
domain.  Independent exact validators enforce the full CP63 and two-macrostep
key rosters, built-in leaf types, source pins, accepted digests, counts, and
fresh/historical flags before either component can enter the subject digest.
Consequently a caller cannot replace a component with a differently formatted
or self-signed mapping.

The deterministic candidate route receipt is
`eab918f4aa9a58f56466673f5e8bcaefb6180692acbdfd9a6e6a694c2a3b6c4f`.
Its route-subject digest is
`a1fb05402feb3efbf7a413e36e3eafc6528ec811ce9768d2bf49332e57d67a03`.

## Custody and hostile qualification

All consumed project files are opened through descriptor-relative no-follow
walks.  Each leaf must be an exact regular `0644`, single-link file with the
accepted byte count and SHA-256, and its full identity must remain stable across
capture.  Canonical absolute-root and relative-path text are enforced.

The focused suite covers omission, substitution, extra keys, re-signed forged
components, stale V1 whole-method hashes, a NO-GO review, false structural
admissibility, internally resealed but wrong chain values, duplicate/nonfinite
or malformed JSON, noncanonical paths, symlink/hardlink/mode custody, cache
poison including explicit `None`, exact bool/int aliases, concurrent execution,
and an unrelated physical project copy.  Qualification is offline and disables
Python bytecode and pytest cache writes.

## Exact zero effects

- proposed timetable closures: **1**;
- author-applied timetable closures: **0**;
- formal-test, field, blocker, result, B12, runtime, data, training, science,
  production, authority, and claim deltas: **0**;
- tracker or ledger edits: **none**.

An independent reviewer must reproduce the validator and tests before the one
proposed timetable task can be registered.
