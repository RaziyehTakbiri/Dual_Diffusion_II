# Solo Block 2 public-documentation reconnaissance amendment v2

**Package status:** `PUBLIC_DOCUMENTATION_RECONNAISSANCE_AMENDMENT_FROZEN_FETCH_HOLD`  
**Project state:** `STATIC_QUALIFIED_NOT_EXECUTABLE`  
**Reported date:** `2026-08-31`  
**Root-page fetches performed:** `0/2`  
**Durable intents created:** `0/2`  
**Scientific effect:** zero

## 1. Exact authority and the mandatory fetch hold

The normalized visible authority text is exactly:

> Sounds good, go through those deferred items in Solo Block 2.

It is 61 UTF-8 bytes and has SHA-256
`80bfdee90ba72bfb1de81058945001cbe5aca1931491636b474d419d93c08c6f`.
Only trailing transport framing or an HTML-space transport entity is outside
the normalized text. Raw transport bytes, account identity, timestamps,
conversation-envelope metadata, and cryptographic user authentication are not
bound.

In its conversational context, this assent authorizes construction and
read-only local review of this bounded six-file amendment/transcript-simulator
qualification package and preparation for the
previously described read-only official-source and license verification. It
does **not** authorize an HTTP fetch now. The final machine raw/semantic and
package-aggregate digests, per-row operational request digests, operational
GO/authority schemas, a content-
addressed network runtime, an admitted raw-custody executor, and an independent
GO did not exist when the assent was given. The amendment freezes only inert
request designs and a pure supplied-transcript simulator. The earlier assent
does not retroactively bind any future executable bytes or operational receipt.
Both operations are therefore
`HOLD_PENDING_SEPARATE_RUNTIME_CLOSURE_WITH_NEW_EXECUTOR_AND_FRESH_REVIEW_AUTHORITY_INTENT`.
A later runtime-closure amendment must introduce and independently review a new
non-injectable executor before it can define any operational GO, authority,
intent, outcome, custody, environment, or request digest. This package creates
none of those receipts.

The assent does not authorize administrative messages, email, support tickets,
forms, approvals, authentication, credentials, cookies, downloads, archive or
file access, APIs, data access, escrow operations, entropy, training, runtime,
scientific execution, results, claim promotion, submission, or tracker edits.
An outcome cannot manufacture any missing approval or authority.

The six additive files are:

- `PROJECT_SOLO_BLOCK2_PUBLIC_DOCUMENTATION_RECONNAISSANCE_AMENDMENT.md`;
- `research/fixtures/manuscript_v3_solo_block2_public_documentation_reconnaissance_amendment_v2.json`;
- `research/diagnostics/manuscript_v3_solo_block2_public_documentation_reconnaissance_amendment_v2.py`;
- `tests/unit/test_manuscript_v3_solo_block2_public_documentation_reconnaissance_amendment_v2.py`;
- `src/heterodiff/artifacts/solo_block2_public_documentation_reconnaissance_executor_v2.py`; and
- `tests/unit/test_solo_block2_public_documentation_reconnaissance_executor_v2.py`.

No existing package, tracker, preregistration, source, manuscript, result, or
custody byte is changed.

## 2. One-way custody and current project state

The machine record live-binds the immutable execution preregistration,
preexecution closure, prospective seal, and the complete previously accepted
static-selection, local-candidate, Retail-split, PhysioNet-split, real-domain
power-route, and pilot-power package quartets. Those are read-only
dependencies. The current completion timetable and evidence ledger are bound
only as historical snapshot receipts: they are mutable downstream trackers and
are not reopened by this validator. A future tracker may consume an
independently accepted amendment one way; this package never reverse-binds a
later tracker digest.

At the tracker snapshot represented here, 152 fields are open and 20 are
closed, all 12 blockers are open, results are `0/4`, and F172 is null. The
seven original Solo Block 2 operational boxes remain open:

1. populate the exact finite precontact instance;
2. independently review and admit it;
3. record authority for the exact administrative-contact roster;
4. open authorized administrative requests;
5. bind all required approval receipts;
6. populate, review, and admit the later data-access instance; and
7. record fresh data-access authority.

This amendment closes none of them. It closes no field, blocker, formal test,
result, domain-admission, source-selection, or scientific gate. Its only
effect is to freeze and locally qualify two precisely bounded root-page designs.
It does not make either row eligible. A separate independently reviewed exact-
runtime closure amendment is required before later executor admission, GO,
fresh row authority, and intent gates can even be evaluated.

### Narrow v1 supersession

The bound v1 candidate deliberately set
`authority_provenance.dataset_page_browsing_authorized`,
`authority_provenance.documentation_license_or_governance_browsing_authorized`,
`candidate_selectors.reconnaissance_or_target_amendment_permitted`, and
`gap_inventory.documentation_license_governance_reconnaissance_exception_permitted`
to false. The new user scope review occurred before any fetch, contact, intent,
outcome, or terminal event. It narrowly supersedes those predicates only to
permit later consideration of the two frozen root-page rows after every new v2
gate. Current browsing authority remains false. The v1 predicates
`gap_inventory.target_mismatch_permits_amendment_or_reconnaissance=false` and
`failure_and_state_contract.terminal_no_go_permits_retry_repair_replacement_fallback_deletion_reacquisition_or_amendment=false`
remain unchanged. There is no mismatch, post-failure, third-target, mirror,
fallback, child-page, or search amendment.

## 3. Exactly two frozen root-page operations

The operation roster is closed and has exactly two rows:

| Ordinal | Domain | Exact URL | Method | Attempts | Current state |
|---:|---|---|---|---:|---|
| 0 | PhysioNet | `https://physionet.org/content/challenge-2012/1.0.0/` | one HTTPS `GET` | 1, zero retry | HOLD |
| 1 | UCI Online Retail II | `https://archive.ics.uci.edu/dataset/502/online+retail+ii` | one HTTPS `GET` | 1, zero retry | HOLD |

The rows are forward-only, not parallel. Row 0 must be attempted first. Row 1
cannot receive a GO, authority, or intent until row 0 has the exact terminal
state `TERMINAL_ROOT_PAGE_OBSERVED_UNVERIFIED_NO_RETRY`; its GO, authority, and
intent must each bind the validated row-0 outcome SHA-256. Any other row-0
state, including a spent intent without outcome, permanently puts row 1 in
`TERMINAL_PREEMPTED_BY_ROW0_NO_REQUEST_NO_INTENT`. Row-0 success would be
necessary but never sufficient authority for row 1: row 1 would still need its
own later GO, new-executor admission, fresh authority, and durable intent.

There is no third URL, wildcard, mirror, fallback, alternate host, search
query, redirect target, child page, link-follow operation, archive locator, or
API route. Each inert request design has an empty body and exact frozen headers.
A future executor must disable redirect following at its client boundary and
treat every 3xx response as terminal.
Authentication, cookies, credentials, forms, `Range`, `Referer`, conditional
requests, scripts, subresources, DNS preflight/probe operations, robots fetches,
HEAD requests, and content negotiation beyond the exact frozen headers are
forbidden. A browser abstraction is `HOLD` unless it can prove redirect
disablement and expose the required raw response receipts.

The exact inert request designs fix HTTP/1.1, method, URL, empty body, and the
ordered `Host`, `User-Agent`, `Accept`, `Accept-Encoding`, `Cache-Control`,
`Pragma`, and `Connection` header bytes. They also record future minimum TLS,
timeout, response-ceiling, zero-redirect, and zero-retry constraints.
`Accept-Encoding: identity` is a future prohibition on transparent content
decoding. The design byte strings and their hashes are useful only as inputs to
offline transcript simulation. They are not emitted requests and are not
operational final-request digests.

No HTTP client, TLS implementation, CA bundle, resolver, socket, loaded native
runtime, or raw-custody sink is admitted by this package. All operational
client/CA/environment/request-digest slots are null. A later runtime-closure
amendment must replace or explicitly supersede the inert design with exact
loaded-runtime and emitted-byte receipts. No operation is eligible while those
receipts and the other future gates are absent.

## 4. Durable intent and outcome state machine

Each row begins in `FROZEN_HOLD`. It may become
`ELIGIBLE_FOR_INTENT_RESERVATION` only after a separate exact-runtime closure
introduces a new executor, raw-custody qualification, an independent GO, and
fresh exact post-review authority
bind the exact raw/semantic/aggregate package digests, row ordinal, and final
exact request digest. Eligibility is not an execution approval.

This package deliberately chooses a dormant-design boundary:
`exact_runtime_admitted=false` and `fetch_eligible=false`. It contains no
production network entrypoint, resolver, socket, TLS connection, send/receive
path, caller-injected transport callable, or canonical write path. Its pure
simulator accepts only the exact frozen `InertTranscript` value object and, for
row 1, the exact row-0 transcript plus its claimed modeled outcome. The
simulator must recompute the entire row-0 modeled outcome from that transcript
and require recursive type-and-value equality, including rejection of Boolean-
integer and integer-float aliases; a self-digest alone is not authentication.
Transcript leaves
are strict strings, exact integers, bytes, tuples of strings or bytes, and
nulls; no callable, general object, URL override, path, file descriptor, or
client may be supplied. It returns in-memory design-validation results. Any
source, interpreter, CA, or
environment observation is outside operational admission. A later closure must
bind the interpreter and imported standard-library bytes; loaded SSL, socket,
hash, TLS/crypto/C and dynamic-loader libraries; CA and resolver configuration;
kernel/OS/architecture and network-affecting environment; non-injectable
production dependency identities; and exact custody semantics. That later
closure cannot retroactively authorize a request: new row GO and authority are
still required.

The dormant transcript simulator owns only exact inert transcript-input and
in-memory simulation-result schemas. Its results are
`TRANSCRIPT_SIMULATION_ONLY_NONOPERATIONAL` and cannot be promoted.
Operational GO, authority, intent, and outcome schema versions, key orders,
type maps, affirmative authority text, paths, and digests are all strict null
here. The custody mechanics below are mandatory minimum design constraints for
a later runtime-closure amendment, not a claim that an operational record
schema is presently admitted.

Here, “final v2 digest” is never used as an ambiguous machine token. The
machine semantic `record_sha256` is computed with that field null; after all
six package files are final, an independent reviewer separately computes the
raw machine-file SHA-256. A downstream aggregate hashes a canonical record
containing both machine digests and the raw hashes of the human amendment,
validator, amendment test, dormant transcript simulator, and simulator test.
Embedding the
machine raw digest or aggregate in the machine itself is forbidden because it
would form a cycle.

A later runtime closure must freeze exact operational GO and fresh-authority
schemas, canonical self-digests, byte-reopening predicates, chronology rules,
and an exact rendered affirmative authority template. Token presence or a hex
digest alone can never be authority. No current user text, simulated result,
or qualification artifact can fill those null schemas.

The remaining custody state machine is a model, not a qualification claim.
The shipped simulator has no filesystem side effects and cannot demonstrate
`openat`, `O_EXCL`, mode, ownership, link-count, or `fsync` behavior. A future
runtime-closure amendment must independently implement and qualify every
mechanic below before any operational schema can become nonnull.

Intent reservation is one attempt only. A future implementation must use
`O_WRONLY|O_CREAT|O_EXCL|O_NOFOLLOW`, mode `0600`, write exactly one canonical
JSON record plus LF, verify a regular file with link count one and the expected
owner, `fsync` the file, close it, and `fsync` its already-open parent
directory. It must never overwrite, truncate, rename over, repair, or replace
an intent. The canonical intent binds the prior chain head, exact machine raw
and semantic digests, aggregate digest, authority digest, row/request digests,
all environment receipts,
attempt limit one, retry limit zero, and all prohibitions.

Once an intent is durable, the attempt is spent even if no request is emitted,
the process crashes, the connection fails, custody is incomplete, or outcome
writing fails. There is no retry, resume, replacement, top-up, alternate URL,
or new intent for that row. Absence of a durable outcome after a durable intent
is terminal `SPENT_INCOMPLETE_NO_RETRY`.

One future hash-linked outcome must be written with the same exclusive-create,
`0600`, regular-file, link-count, ownership, file-`fsync`, and parent-`fsync`
requirements. It binds the exact intent digest and prior chain head. A later
outcome may report only what the one spent attempt observed; it can never
alter authority, create an approval, verify a source, select a dataset, close
a field, or make a second request eligible.

The same future guarantees apply to exact `raw-request`, `raw-response-head`,
`raw-transfer-body`, `raw-metadata`, `raw-stderr`, and derived
`decoded-entity-body` sidecars, using the frozen row-relative names. Every raw
sidecar is exclusively created as an empty `0600` regular file and durably
fsynced before the request; bytes may then flow only through already-open file
descriptors or non-injectable internal custody sinks. Client path reopening,
truncation, or overwrite is
forbidden. Device, inode, owner, mode, and link count are verified before and
after; each file and the parent directory are fsynced before outcome creation.
The raw head retains the exact status/header bytes with offsets, the raw body
retains transfer framing, and any dechunked text is a separate derived receipt.
Status, headers, raw body, metadata/effective URL, stderr, decoded entity, and
request bytes each have an independently enforced frozen ceiling. Until a
later, independently reviewed executor proves these mechanics, the fetch route
remains HOLD.

The custody root is not guessed or created by this package: its absolute path,
device, inode, owner, and `0700` mode, plus the exact single-component row
basename, are strict null now and must later be bound by a newly admitted
executor, GO, fresh row authority, and intent. That executor must preopen the exact no-
follow root directory descriptor, verify the bound identity, create the exact
row directory with `mkdirat` at mode `0700`, reopen it
with `openat` plus `O_DIRECTORY|O_NOFOLLOW`, and create every basename relative
to that descriptor. Slash, `..`, NUL, empty components, symlink traversal, and
ambient/pathlib re-resolution after root open are forbidden. Root and row directory owner, mode,
device, inode, and link count are checked; the intent binds both executor-
admission receipt path and digest.

The terminal precedence is:

1. protocol or custody violation -> `TERMINAL_PROTOCOL_VIOLATION_NO_RETRY`;
2. durable intent without a complete durable outcome ->
   `TERMINAL_SPENT_INCOMPLETE_NO_RETRY`;
3. attempted redirect, target/final-URL mismatch, third URL, child page,
   search, form, authentication, cookie, download, API, or data access ->
   `TERMINAL_SCOPE_VIOLATION_NO_RETRY`;
4. transport/status/type/size/content/custody failure ->
   `TERMINAL_TRANSPORT_OR_CONTENT_NO_GO_NO_RETRY`;
5. exact bounded root-page success ->
   `TERMINAL_ROOT_PAGE_OBSERVED_UNVERIFIED_NO_RETRY`.

No terminal branch is permission for another network operation.

## 5. Exact response custody and bounded success

A future operational outcome schema must require raw custody of the
protocol/status representation,
raw header block, encoded response body, and exact effective/final URL, plus
their byte counts and SHA-256 digests. It records redirect count, timing,
peer/TLS receipt digests, declared and normalized media type, truncation state,
client exit status, and every failure predicate. All current intent, response,
outcome, and observation slots are strict null.

Success is deliberately narrow. It requires all of the following:

- exactly one attempted HTTPS GET under the durable intent;
- status `200`;
- exact final URL byte-for-byte equal to the row URL;
- redirect count zero and no redirect response;
- complete raw status, raw headers, body, and final-URL custody;
- body size at or below the frozen encoded-body ceiling, with no truncation;
- exactly one valid media type in `text/html`, `application/xhtml+xml`, or
  `text/plain` after ASCII case normalization and parameter removal;
- exactly one `Content-Type`, zero `Content-Disposition` (including inline or
  attachment), and zero `Location` headers;
- nonempty UTF-8 text with no BOM and no prohibited control bytes; and
- no forbidden archive, database, columnar, model, serialized-object, or
  compressed-data magic prefix; and
- false login-wall, consent-wall, challenge-page, robot-block, and error-page
  detectors, with no exact rejection substring match.

A raw transfer-coded body is retained exactly. Any semantic UTF-8 or magic
check operates only on a separately receipted offline dechunked entity body;
if exact dechunking is unavailable or ambiguous, the row is no-go. A `200`
transport success is not official-version verification, license
verification, governance approval, archive verification, source selection,
data access, or populated-instance success. Any login wall, consent form,
challenge page, robot block, error page, ambiguity, missing fact, or content
mismatch remains no-go; no form or child link may be opened to investigate.
An ambiguous or unparseable header or framing condition is specifically a
top-precedence protocol violation. Only an ambiguous or unparseable page or
content-classification result falls under the later transport/content terminal
branch.

The inert simulator exposes one authoritative ordered 36-field outcome-
diagnostic roster with exact strict types. The machine record binds that roster
and its canonical digest byte-for-byte: field names, order, and types must be
identical, with no alias, omission, or extra. The emitted in-memory outcome uses
all 36 names. This is an inert-simulation schema only; the future operational
outcome schema remains strict null and unadmitted.

For every supplied transcript, all supplied bytes undergo complete protocol
and framing validation before scope, status, or content classification. A
connection-close-delimited body is complete only when the immutable transcript
contains exactly one final inert EOF event. A missing, duplicate, or nonfinal
EOF event is a top-precedence protocol failure, never an accepted partial body.
`body_truncated` means only that supplied body or decoded-entity bytes were not
fully retained because a frozen byte ceiling was reached; it is not a generic
rejection flag. Once UTF-8 decoding succeeds, `body_utf8_valid` remains true
even if a later scope, status, or content classifier rejects the transcript.

## 6. Offline-only extraction candidates

Only after a complete terminal root-page outcome exists may a separate local
reader inspect its already-custodied text bytes. It must perform no network
operation, script execution, rendering fetch, link following, archive opening,
or external parser action. Every extracted item must preserve the raw UTF-8
value, zero-based half-open byte offsets into the exact body, body SHA-256,
field name, and the literal status
`UNVERIFIED_CANDIDATE_NOT_CONTACTED`.

The only allowed semantic fact fields are:

- `displayed_source_title`;
- `displayed_dataset_identifier`;
- `displayed_version_or_revision`;
- `displayed_publication_or_update_text`;
- `displayed_license_label`;
- `license_url_candidate`;
- `terms_url_candidate`;
- `documentation_url_candidate`;
- `archive_url_candidate`;
- `displayed_access_prerequisite_text`;
- `displayed_governance_or_ethics_text`;
- `displayed_retention_or_redistribution_text`;
- `displayed_schema_metadata_text`;
- `displayed_timezone_metadata_text`;
- `displayed_contact_name_candidate`;
- `displayed_contact_role_candidate`;
- `displayed_contact_email_candidate`; and
- `displayed_contact_url_candidate`.

Candidate URLs, email addresses, names, and roles are inert strings, not
approved targets or an admitted contact roster. They cannot be opened, sent,
or promoted by this package. Extraction cannot fill
the preregistration's version, snapshot, hash, license, governance, approval,
split, or escrow slots and cannot promote any value to verified, accepted,
official, selected, contacted, approved, or complete. A distinct later review,
additional authority, and the still-open precontact-instance process would be
required for any promotion.

## 7. Qualification boundary

The validator is read-only. It enforces exact top-level and nested schemas,
types (including rejection of booleans as integers), canonical JSON bytes,
self-digest, package hashes, immutable-input hashes/modes/link counts, authority,
two-row closure, request and failure semantics, strict-null observations,
historical tracker receipts, and zero scientific effect. Its source is AST-
screened against network, URL, browser, process, entropy, dynamic execution,
and write APIs. Hostile tests exercise authority broadening; a third URL;
redirect, child, search, form, auth, cookie, download, API, data, and retry
routes; outcome/approval promotion; tracker/science/field/box closure; schema,
type, extra-field, canonicalization, self-digest, hash, source-safety, and
unrelated-working-directory failures.

Software qualification supplies immutable response transcripts directly to a
pure in-memory simulator. It may return explicitly labeled modeled intent,
outcome, and custody dictionaries, but neither it nor its tests materialize any
filesystem, canonical, or operational intent, outcome, custody, authority, GO,
or contact record. There is no callable transport seam. The suite uses
`python -B` and disables pytest's cache provider. It performs no web request
and has no canonical operational or scientific effect.

The stopped replacement simulator source is exactly 91,141 bytes with SHA-256
`9387a1fdab15cb808f6271ec04bd8ff749222561e6bdfe3b33ed69700633ec7f`;
its stopped hostile test is exactly 65,794 bytes with SHA-256
`d7718c516d3ce57de2195b19eb8d617bb2bcbd8aec4003da7144c1f6c489665e`.
The simulator's internal semantic contract digest is
`0bd86fe3b851603e68ea642619645e71334a8f689be8d97438490d04a51fe9f2`
and its exact two-row operation-roster digest is
`5f305448d4032b55dac54057d2d659212dd512e65113c1123409aa3c089b7548`.
Its exact ordered 36-field outcome-diagnostic roster digest is
`a8fdba4d39e97ac3fbf23d065ae2bd9805cc0513f77d2232c23c7bf4799966dd`.
These receipts bind an inert transcript model only; they are not runtime,
client, custody, request, GO, authority, intent, or outcome admission.
