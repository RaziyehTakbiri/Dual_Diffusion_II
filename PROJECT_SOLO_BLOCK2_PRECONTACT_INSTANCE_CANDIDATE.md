# Solo Block 2 precontact-instance candidate and gap audit

**Package status:** `PRECONTACT_INSTANCE_LOCAL_CANDIDATE_COMPLETE_AWAITING_PRECONTACT_PREREQUISITES_AND_INDEPENDENT_REVIEW`  
**Project state:** `DRAFT_NOT_EXECUTABLE`  
**Reported date:** `2026-08-30`  
**Candidate present:** yes  
**Populated precontact instance present or admitted:** no  
**Precontact population blocked:** yes  
**Dataset, documentation, license, governance, approval, or access contact by this package:** none  

## 1. Scope, authority, and exact downgrade

The normalized visible authority text for this additive package is exactly two
lines:

> Okay, go ahead to the next step then.  
> And dont forget to mark the steps carried out in the project plan.

Its UTF-8 SHA-256 is
`d5d42ebe4def8e706729637627e2497f7b62f1b239a4f49e74e40f488f8b166a`.
Only trailing transport `&#x20;` entities and transport whitespace were removed.
Raw transport bytes, conversation-envelope metadata, account identity,
timestamps, and cryptographic user authentication are not bound.

This text authorizes the next bounded static planning artifact and, only after
independent validation, later one-way project-plan maintenance. It does not
authorize contacting a dataset source, documentation page, license office,
governance body, approval target, or access endpoint; browsing a dataset page;
creating an approval; using credentials; opening or downloading data; running
a protocol; splitting data; opening escrow; scientific execution; or filling
any result or preregistration field. The four filenames, schema, candidate
operation rows, role tokens, split proposal, and exact machine tokens are
agent-selected implementation details rather than user-selected facts.

The requested populated instance cannot honestly be issued from the local
evidence. This package is therefore deliberately downgraded to a **local
candidate and gap audit**. It enters only
`PRECONTACT_INSTANCE_LOCAL_CANDIDATE_COMPLETE_AWAITING_PRECONTACT_PREREQUISITES_AND_INDEPENDENT_REVIEW`.
It is not a populated or reviewed instance and cannot advance the predecessor
protocol state machine.

The four new files are:

- `PROJECT_SOLO_BLOCK2_PRECONTACT_INSTANCE_CANDIDATE.md`;
- `research/fixtures/manuscript_v3_solo_block2_precontact_instance_candidate_v1.json`;
- `research/diagnostics/manuscript_v3_solo_block2_precontact_instance_candidate_v1.py`;
- `tests/unit/test_manuscript_v3_solo_block2_precontact_instance_candidate_v1.py`.

No existing preregistration, seal, static-selection package, tracker,
manuscript, source, authority record, runtime record, or result byte is changed.
The original populated-instance project-plan checkbox remains open.

## 2. Input custody and one-way dependency

The read-only validator reopens exactly ten immutable inputs: the execution
preregistration, its preexecution closure, the four-file prospective no-test-
data seal, and the four-file Solo Block 2 static-selection freeze. It verifies
their exact bytes, modes, link counts, and relevant self-digests. Those inputs
continue to report 172 unresolved fields (166 preexecution plus six deferred),
12 blockers (10 execution plus two submission), zero closures, and no data or
contact authority.

Six manuscript and source files informed this local drafting exercise. Their
hashes are historical snapshot receipts only. The validator never reopens
them, and they are not official source, license, version, archive, governance,
or data evidence. Mutable trackers are not bound. A future tracker may consume
this package after an independent GO; this package never reverse-binds a future
tracker digest.

Later legitimate precontact records are not forbidden by a permanent absence
check. They require a separately reviewed populated instance and fresh scoped
authority. This candidate supplies neither.

## 3. Candidate identity and strict-null observations

The candidate is present only as a static proposal. All of the following are
false: populated instance present, admission, independent review, admin-contact
authority, data-access authority, intent reservation, contact, access,
snapshot, split, escrow activation, scientific execution, and result creation.

The predecessor's thirteen future-observed fields remain strict null:

1. administrative contact outcomes;
2. data-access outcomes;
3. observed snapshot versions;
4. raw snapshot SHA-256 values;
5. snapshot byte counts;
6. ETags or equivalents;
7. license-text receipts;
8. governance-approval receipts;
9. ethics-approval receipts;
10. split counts;
11. split-manifest SHA-256;
12. escrow receipts; and
13. the access-log head SHA-256.

No local guess or copied text fills any of these slots.

## 4. Sole registered targets and the candidate four-row core

The only registered dataset targets are copied from the immutable
preregistration and remain unverified identifiers:

- PhysioNet Challenge 2012:
  `https://physionet.org/content/challenge-2012/1.0.0/`;
- Online Retail II:
  `https://archive.ics.uci.edu/dataset/502/online+retail+ii`.

This package did not resolve, open, browse, probe, or contact either URL.
Redirects, alternate mirrors, replacement versions, fallback sources, wildcard
targets, and later row insertion are not permitted by this candidate. A target
or selector mismatch would be terminal under the frozen design, not permission
for reconnaissance or amendment.

The candidate core contains exactly four contiguous, single-attempt,
zero-retry rows:

| Ordinal | Domain | Phase | Exact target | Candidate request |
|---:|---|---|---|---|
| 0 | PhysioNet | ADMIN | registered PhysioNet URL | metadata/license/governance questions only |
| 1 | Retail | ADMIN | registered Retail URL | metadata/license/governance questions only |
| 2 | PhysioNet | DATA | one content-addressed archive derived from exact row-0 success | ineligible data-authentication/download candidate |
| 3 | Retail | DATA | one content-addressed archive derived from exact row-1 success | ineligible data-authentication/download candidate |

Each future operation would require a durable `O_EXCL` 0600 intent file,
file fsync, parent fsync, and an append-only hash-linked outcome. Intent without
outcome is terminal spent/incomplete with no retry. The exact row success
predicate and required prior receipts are frozen in the machine record. Failure
disposition is resolved in exact order: protocol violation first; durable
intent without an outcome second, always to
`TERMINAL_SPENT_INCOMPLETE_NO_RETRY`; the global named failure map third; and
the row's residual ADMIN or DATA non-success disposition last. A single phase
disposition never overrides spent-incomplete, selector/snapshot, or approval-
specific mappings.

The two ADMIN rows may request only identifiers, version/archive metadata,
license/terms, governance/ethics requirements, access prerequisites, retention
and redistribution constraints, and schema/timezone metadata. They may not
authenticate, download protected bytes, request outcomes, or create an
approval. Exact admin success requires one and only one immutable archive
locator plus an exact version/revision, SHA-256, byte count, governing terms,
and requirements receipt. Zero or multiple archives, redirects, missing
identity fields, or mismatch are no-go.

The two DATA rows are structurally present only to expose the eventual
dependency. They are ineligible unless the relevant ADMIN row is an exact
success, every required approval receipt exists and validates, a separately
reviewed access instance exists, fresh data-access authority exists, and a new
durable intent is reserved. None of these predicates is currently true.

The four-row core is **not complete for admission**. It omits concrete contact
targets that may be required to obtain license, governance, ethics, security,
or independent approval receipts. No undeclared approval contact is permitted.
The contact roster may be completed by an explicitly authorized scope review
only **before any contact occurs**. If an ADMIN outcome after contact reveals a
required contact that was not already frozen, this protocol is terminal no-go.
No later scope review may repair, resume, insert, replace, or extend an
operation within this protocol/project after contact or terminal no-go.

## 5. Candidate administrative questions

For each registered source, the proposed ADMIN request is limited to seven
questions:

1. What is the canonical dataset identifier and current immutable version or
   revision for the registered target?
2. Is there exactly one immutable archive locator, with SHA-256 and byte count,
   for that version?
3. What exact license or terms govern access, storage, analysis, publication,
   redistribution, and retention?
4. What account, authentication, or data-use-agreement requirements apply?
5. What governance, ethics, privacy, clinical, or institutional approvals are
   required before access?
6. What storage, deletion, retention, disclosure, and publication controls are
   mandatory?
7. What schema and timezone metadata are required to evaluate the frozen
   selector and split rules deterministically?

These are static question strings, not sent requests. Current official answers
remain future administrative observations. There is no documentation-page or
license-page reconnaissance exception before a future admitted protocol,
fresh authority, and durable intent.

## 6. Candidate acquisition selectors

The PhysioNet selector proposes the agent-selected, locally copied identity
tokens `physionet-challenge-2012` and `1.0.0` at the exact registered URL. It
requires exactly one immutable archive locator, SHA-256, and byte count in an
accepted future ADMIN receipt. These tokens are unverified by this package.

The Retail selector proposes the agent-selected dataset identifier `502` and
title token `online-retail-ii` at the exact registered URL. The concrete
immutable Retail version and archive are unknown. A future ADMIN receipt must
return exactly one version/revision, archive locator, SHA-256, and byte count.
No current Retail version or archive is invented here.

Both selectors are fail closed: no redirect, mirror, fallback, closest match,
version substitution, or post-outcome target amendment. Any mismatch enters
`SELECTOR_OR_SNAPSHOT_TERMINAL_NO_GO`.

## 7. Candidate split proposal and unresolved Retail seam

The agent-selected candidate proportions are exactly 70/100 training, 15/100
validation, and 15/100 test. They are not power-justified and close no power,
threshold, field, blocker, or formal-test obligation. A future admissible rule
would use deterministic Hamilton apportionment over natural groups, minimum
five groups (the smallest count at which 70/15/15 Hamilton allocation can make
all three splits nonempty), fixed tie priority `TRAIN`, `VALIDATION`, `TEST`,
and a public domain-separated SHA-256 ordering with no secret seed or entropy.
The literal hash domain separator, canonical group-ID byte encoding and
normalization, and collision tie-break are not yet populated, so the overall
split algorithm is not exact.

For PhysioNet, the natural group is a nonempty canonical patient identifier.
Every record for a patient must remain in one split; duplicate or cross-split
patient membership is terminal. This grouping policy is fixed, but the exact
patient ordering/hash algorithm is not populated because the literal domain
separator, canonical patient-ID bytes, and collision rule are missing.

For Retail, the immutable preregistration requires
`CUSTOMER_DISJOINT_AND_TEMPORAL` and separately forbids test-set exclusion.
A customer may span a proposed time cutoff. Quarantining, censoring, dropping,
or moving those rows after observing data would violate that no-exclusion
boundary. Therefore this package does **not** claim that a complete Retail
temporal rule has been populated.

The only candidate feasibility idea retained for later review is to build each
customer's complete UTC interval `[minimum_invoice_time, maximum_invoice_time]`
and search deterministic boundary pairs such that every full customer interval
lies wholly in exactly one chronologically ordered window, customer sets are
disjoint, and the exact Hamilton group counts are met. No customer, invoice,
or row may be censored, excluded, split, or reassigned. If no such pair exists,
the outcome is terminal no-go. Because this rule has not been independently
reviewed for consistency with all preregistered temporal and no-exclusion
requirements, `retail_exact_temporal_rule_populated=false` and population is
blocked.

Actual snapshot hashes, counts, observed versions, UTC cutoffs, split counts,
and split-manifest hashes remain future-observed nulls.

## 8. Approval and escrow gaps

A future admitted instance must bind exact receipts for source terms and
license; PhysioNet clinical/ethics/governance determination; Retail privacy,
pseudonymous-customer, duplicate, and membership-inference determination;
security, storage, retention, deletion, redistribution, and publication
controls; the exact source manifest; independent protocol review; and a later
data-access-instance review.

The concrete approval targets, reviewer identities, official endpoints, and
receipt validators are not locally bound. This is a local/user-resolvable
precontact prerequisite, not permission for reconnaissance. Population is
blocked until exact targets and rules can be frozen locally or through a newly
authorized scope review that does not contact the source.

The candidate names four role tokens:

- `RAW_SNAPSHOT_CUSTODIAN_ROLE`;
- `DETERMINISTIC_SPLIT_OPERATOR_ROLE`;
- `INDEPENDENT_HELD_OUT_ESCROW_CUSTODIAN_ROLE`; and
- `INDEPENDENT_FINAL_OPENING_APPROVER_ROLE`.

These are labels, not real people, accounts, keys, ACLs, or accepted duties.
No independent escrow exists. A solo worker assigning all roles to themself
must not be called independent. Real principals, key identities, ACL acceptance,
and final-opening authority are required before population or admission.

## 9. Exact gap partition and terminal behavior

Local or user-resolvable precontact prerequisites that block population are:

- a power justification for the proposed split;
- an exact PhysioNet hash domain, canonical patient-ID encoding, and collision
  tie-break rule;
- real escrow principals, keys, ACLs, and acceptance;
- concrete approval/reviewer target identities and validation rules; and
- an independently reviewed exact Retail temporal rule consistent with
  customer disjointness and the no-exclusion boundary.

Future administrative observations, which may be learned only through the
already frozen exact ADMIN rows after a future admitted protocol, fresh
authority, and durable intent, are:

- current official source version/archive/license/governance requirements;
- the concrete immutable Retail version and archive; and
- one PhysioNet immutable archive locator, hash, and byte count.

These future observations are not prerequisites to browse or contact before
the protocol. If a fact is needed to define the first exact contact target or
rule and is not locally bound, population remains blocked.

The predecessor's ten named failure mappings remain exact. Denied, failed, or
cancelled ADMIN operations enter `ADMIN_CONTACT_TERMINAL_NO_GO`; incomplete
approvals enter `APPROVALS_INCOMPLETE_TERMINAL_NO_GO`; unavailable versions or
selector/snapshot mismatches enter `SELECTOR_OR_SNAPSHOT_TERMINAL_NO_GO`; and
denied, failed, or cancelled DATA operations enter
`DATA_ACCESS_TERMINAL_NO_GO`. Protocol violation has precedence. Unknown or
missing outcomes never count as success. No terminal state permits repair,
retry, replacement, fallback, deletion, reacquisition, or selector amendment.

## 10. Effects, scope cap, and anonymity boundary

After exact validation, this package may support only the named project-control
predicate
`SOLO_BLOCK2_PRECONTACT_INSTANCE_LOCAL_CANDIDATE_AND_GAP_AUDIT_VALIDATED`.
It does not close the original populated-instance checkbox. It closes zero of
172 unresolved fields, zero of 12 blockers, zero formal tests, zero results,
and no C17, CKS, common-support, domain-admission, method, runner, power, or
production gate.

This is the second and last preparatory package targeting precontact under the
current scope review. A third micro-layer requires explicit new scope review.
Later project-plan maintenance is one-way and only after independent GO; no
tracker digest is part of this package.

The quartet is internal research custody evidence. It is not an anonymous or
public manuscript supplement. A publication-safe derivative would need a
separate review and must omit local custody details, access controls,
credentials, person/account identities, raw receipts, and protected-data
metadata. This package contains no credential, token, key material, data path,
local absolute path, protected outcome, or scientific result.

## 11. Definition of done for this candidate only

The candidate package is complete only when:

- all four additive files are canonical, closed-world, mutually bound without
  a self-cycle, mode 0644, link count one, and free of local absolute paths;
- the ten immutable predecessor files reopen exactly;
- the candidate state, strict-null observations, incomplete four-row core,
  unresolved Retail seam, prerequisite partition, and all nonclaims validate;
- hostile mutations fail closed, including type aliases, row insertion,
  contact or authority promotion, approval-contact completion, Retail exclusion,
  invented observed facts, escrow promotion, retry, fallback, and tracker
  closure;
- validator and tests expose no network, protocol, authority/runtime, data, or
  scientific route, and all test writes are confined to temporary replicas;
  and
- no bytecode cache is created by the final cache-disabled qualification.

Meeting this definition validates a local candidate and gap audit only. It does
not populate, review, admit, or execute a precontact instance.
