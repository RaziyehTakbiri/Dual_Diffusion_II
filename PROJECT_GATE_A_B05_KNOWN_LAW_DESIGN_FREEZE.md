# Gate-A B05 known-law design freeze

**Date:** 2026-08-31  
**State:** `GATE_A_B05_F007_F018_KNOWN_LAW_DESIGN_FROZEN_PREOUTCOME`  
**Control predicate:** `B05_F007_F018_ALL_OR_NOTHING_KNOWN_LAW_DESIGN_FREEZE_CERTIFIED`  
**Global state:** `DRAFT_NOT_EXECUTABLE`  
**Scientific execution:** none

## 1. Scope and authority

The user's autonomous-work instruction authorizes local project work while the
user is absent. The earlier explicit authorization also permits an additive V4
terminal-PASS custody registration without rerun, runtime approval, or science.
Neither instruction authorizes network or data access, entropy, training, live
runtime, scientific execution, submission, or claim promotion.

This package is one all-or-nothing pre-outcome design freeze. It closes exactly
F007--F018 only because the complete mixed known-law suite, field values,
reference enclosures, and fail-closed qualification contract are present
together. No partial field credit is permitted. The five files are:

- `src/heterodiff/evaluation/mixed_marked_ctmc_ou_known_law_certified_reference.py`;
- `PROJECT_GATE_A_B05_KNOWN_LAW_DESIGN_FREEZE.md`;
- `research/fixtures/manuscript_v3_gate_a_b05_known_law_design_freeze_v1.json`;
- `research/diagnostics/manuscript_v3_gate_a_b05_known_law_design_freeze_v1.py`; and
- `tests/unit/test_manuscript_v3_gate_a_b05_known_law_design_freeze_v1.py`.

The source is immutable in this package at SHA-256
`98ffb1f42bee3efc097f378cc55a00b88f2d8570b9f3e8de1fe5f9a727f2e268`
and 124895 bytes. Its frozen certificate digest is
`e202379f735e76dc43105cff62e4ff443a97ff810d89edecaf8091e5eefe187d`;
the reference-table digest is
`1b6cfa0d42a8e9271af4e78cbdf5f81843dcf3df3507687754b373fa49a2d314`;
and the grid digest is
`f3cdaafcd931dc41b37bf51666ff262c5d8eff25439b27f208ef2680e1464c8e`.

## 2. Exact F007 error ownership

F007 freezes the oriented exact-versus-plugin shared-guide identity

`K_PATH = K0 + KC + K_BIRTH + K_DEATH + K_REPLACEMENT`.

There is no sixth cap term and no KL triangle inequality. The seven
nonoverlapping end-to-end buckets, each counted at most once, are:

1. noninitial, nonterminal target/reference mismatch under one explicit
   oriented law or intermediate-law comparison;
2. analytic and association-guide approximation, excluding every cap effect;
3. residual estimation through `e = r_theta - r_star`;
4. cap restriction or harmonic defect through exactly one selected route;
5. normalized initialization, owned by F016 and `K0` in the shared-guide
   comparison;
6. desired-target versus terminal-reference mismatch, distinct from F017's
   candidate-owned conditioned endpoint validation; and
7. numerical enclosure error, which is an arithmetic budget and not a
   scientific KL summand.

The only allowed cap routes are residual-PDE stability, a projection
decomposition, or an explicitly oriented intermediate-law comparison with a
proved composition inequality. Association-guide error is inside bucket 2,
not an eighth bucket. Finite-RNG/runtime error is outside this theorem-level
seven-bucket decomposition and remains future runtime work.

## 3. Exact F008 pre-D1 A1 gates

F008 projects the complete Section 7.3 table from
`research/62_a1_association_guided_residual_falsification_spec.md`, whose raw
SHA-256 is
`475f4f450cb5703e6773c0d0ff242db995a16408acce5989401fa0674326e67c`.
The literal eleven-row table becomes twelve decision rows because the primary
and refined unconditional denominators are independently strict:

| Literal quantity | Direction and threshold |
|---|---:|
| terminal guide/target log-density error | `<= 1e-12` |
| maximum terminal residual | `<= 1e-10` |
| oracle self path KL | `<= 1e-10` nat |
| primary/refined path-KL change | `<= 1e-8` nat |
| primary/refined unconditional path-KL change | `<= 1e-8` nat |
| primary unconditional denominator | `> 1e-12` nat |
| refined unconditional denominator | `> 1e-12` nat |
| exact-target occupancy maximum absolute error | `<= 1e-8` |
| primary/refined endpoint TV | `<= 1e-8` |
| generator row-sum residual | `<= 1e-10` |
| scalar edit-cycle residual | `<= 1e-10` |
| oracle product-positive maximum absolute logit | `<= 1e-9` |

The exact source sentence is preserved: any numerical-gate failure places the
scientific decision on HOLD. Solver tolerances, SciPy error estimates,
cross-method differences, and historical reference outputs are not these
thresholds.

## 4. F009 composite mixed known-law suite

The primary quantitative subject is cap one with modes EMPTY, ALPHA, and BETA.
Its row-source generator is

`[[-5/6,1/2,1/3],[2/5,-13/20,1/4],[3/5,1/5,-4/5]]`,

with exact stationary probabilities `(282,280,205)/767`. An occupied mode owns
one mark satisfying `dX=-(1/2)X dt+dW` with invariant `N(0,1)`. Birth draws the
destination mark from that invariant fiber, death removes it, and the primary
replacement retains the mark. The observation reference is `N(0,2)`; the
occupied kernels have clutter/mark weights `(1/4,3/4)` for ALPHA and
`(1/2,1/2)` for BETA at the fixed branch `y=0`.

The exact information is

- `h_EMPTY=1`;
- `h_i(t,x)=1-A_i(t)+A_i(t) psi(t,x)`;
- `A(t)=exp((1-t)K)(3/4,1/2)^T`, with
  `K=[[-13/20,1/4],[1/5,-4/5]]`; and
- `psi=sqrt(2/(2-rho^2))*exp(-rho^2*x^2/(2*(2-rho^2)))`,
  `rho=exp(-(1-t)/2)`.

Exact stationarity and `E_nu psi=1` prove the empty backward equation. The
killed substochastic semigroup and `K a=(-29/80,-1/4)` prove
`0<=A_i(t)<=a_i<=3/4`, hence `h_i>=1/4` uniformly. The nonzero reference
perturbation is `e_EMPTY=0` and
`e_i(t,x)=(1-t)(c_i+x/3)` with
`(c_ALPHA,c_BETA)=(1/5,-1/4)`. It terminal-matches exactly and qualifies the
integrator/five-term identity only; it is not a decision candidate.

The same F009 suite contains three separate cap-two structural subcases, with
subcase-local measures that may not borrow evidence from one another:

- an atomic `nu=delta_1` stratum at `2 delta_(ALPHA,1)` proving the true
  `1/2!` orbit mass, two indistinguishable death/replacement routes, and exact
  aggregate detailed balance;
- a continuous Gaussian route-sum replacement `y=2x` with
  `r(y)=(1/2)exp(3y^2/8)`, inverse `x=y/2`, and reverse rate
  `k_BA(y)=(9/16)r(y)`, proving a direction-sensitive nonunit
  destination-fiber RN/Jacobian identity; and
- distinct marks and observations `(-1,1)`, giving identity and swap
  association weights proportional to `1` and `exp(-4)`, both positive,
  unequal, and normalized by their common denominator.

At a named cap-two boundary, both omitted auxiliary cap-three birth families
are present with masses `1/2` and `1/3`, guide ratio two, zero legal cap-two
outward flux, and exact capped-minus-auxiliary harmonic terms `-1/2` and
`-1/3`, summing to `-5/6`. Their positive hypothetical Bregman values are
separate diagnostics, never the harmonic defect and never legal path-KL terms.

The exact equal-prior classifier has ratio `h` and probability
`tau=h/(1+h)`; EMPTY has `tau=1/2`. Its calibration nuisance is fixed to zero.
A separate nonzero classifier nuisance `c(a,m,z)=2/7`, forbidden to depend on
process time or state and never folded into `h`, proves quotient, local-ratio,
and normalized-initializer invariance while changing unquotiented calibration.
An additional terminal-matched potential gauge is explicitly optional and is
not substituted for the classifier nuisance identity.

## 5. F010 immutable evaluation grid

The finite diagnostic grid uses times `0,1/4,1/2,3/4,1`, occupied marks
`-2,-1,0,1,2`, all six legal birth/death/replacement routes, and all four
blocked cap-one birth attempts. Coordinates mean exactly:

- drift and occupied calibration: current occupied mark `x`;
- birth ratio: newly drawn destination mark `z`;
- death ratio: removed source mark `x`;
- retained replacement: the same mark `x` at source and destination; and
- blocked birth: the gridded coordinate is the existing cap-source mark, while
  the attempted destination mark is integrated over its named `nu` fiber.

There are 50 drift cells, 50 cells per legal ratio family, 100 blocked-flux
cells, and 55 calibration cells. Twelve nonoverlapping structural cases cover
atomic multiplicity, continuous RN routes, association, both blocked families
and their signed aggregate, classifier nuisance, and the optional gauge.

The path integration grid is separately and completely bound: composite
Simpson on `[0,1]` with 1024 even subintervals and the ordered 1025 nodes
`j/1024`, including every integer weight. The total bound union has 1392 cells.
The grid digest binds every node, weight, terminal boundary, coordinate role,
cap-two mark/observation, and derivative domain.

## 6. F011--F018 scientific ceilings

A future candidate passes only when the certified upper endpoint of its actual
candidate error is at most the scientific ceiling and the enclosure width is
at most the separate numerical budget. A narrow interval around a large error
does not pass. Missing evidence, a straddling decision interval, support loss,
or a ratio at a structural zero yields HOLD.

| Field/metric | Scientific ceiling | Candidate enclosure-width budget |
|---|---:|---:|
| F011 omnibus target-first KL | `1e-8` nat | `1e-12` |
| F011 omnibus TV | `1e-6` | `1e-12` |
| F011 equal-prior calibration | `1e-8` | `1e-12` |
| F012 covariance-norm drift discrepancy | `1e-8` | `1e-12` |
| F013 birth log-ratio discrepancy | `1e-8` | `1e-12` |
| F014 death log-ratio discrepancy | `1e-8` | `1e-12` |
| F015 replacement log-ratio discrepancy | `1e-8` | `1e-12` |
| F016 initializer KL / TV | `1e-8` nat / `1e-6` | `1e-12` each |
| F017 full candidate-owned endpoint KL / TV | `1e-8` nat / `1e-6` | `1e-12` each |
| F018 full forward path KL | `1e-8` nat | `1e-10` |

F011 is not an independent self-report: its KL is the componentwise interval
maximum of linked initializer, endpoint, and path KL; its TV is the analogous
maximum including a full-path TV certificate. Cap-two predicates are separate
conjuncts, not KL/TV aggregands. F016 is the full normalized exact-first
initializer law with certified normalizers and common support. F017 requires
the full candidate-owned propagated endpoint law, not terminal-potential
equality or moments alone.

For F018, `e=log(hat h/h)`,
`K0=log(Z_hat/Z)-E_{rho0^h}e_0`,
`KC=(1/2)E_{P^h} integral ||sigma^T grad e||^2 dt`, and each legal jump family
is `E_{P^h} integral q^h Phi(Delta e) dt`, where
`Phi(z)=exp(z)-1-z` and `Delta e=e(destination)-e(source)`. Aggregate
multiplicity, destination-fiber RN, and Jacobian enter `q^h` before the
information ratio. Occupation is always under the exact target path law.
Support loss is infinite/HOLD, reverse KL is diagnostic-only, and cap
structural zeros are excluded.

## 7. Rigorous reference certification

All authoritative numerics use exact `Fraction` arithmetic and outward
rational enclosures. Matrix exponentials use nonnegative uniformization with an
explicit Poisson tail. Exponential/logarithm use rational Taylor series with
analytic tails; square roots use integer-root outward bounds. No SciPy
estimate, binary64 agreement, adaptive-quadrature estimate, or reference output
is treated as a bound or threshold.

Each of the six legal path routes carries a topologically derived receipt:
serialized Simpson approximation and fourth Taylor-coefficient range, then
`4!` derivative bound, exact Simpson remainder, unclamped/final enclosure,
target occupation/rate weight, and weighted contribution. The derivative
coefficient interval is a global range, not a narrow arithmetic-precision
interval. Birth, death, and replacement records are exact sums of their two
serialized route contributions; dynamic is the exact four-component interval
sum; total is the exact five-component interval sum. Every required component
has nonnegative lower endpoint.

Reference arithmetic has a third independent budget role. Every drift, ratio,
calibration, association, nonunit-RN, cap-Bregman, initializer, endpoint, path
component, and route-final surface passes its frozen `1e-12` reference-width
budget. The maximum path-component width is
`709652695285949459155360982361705933718973 / 31624703193907200000000000000000000000000000000000000000`,
and the maximum route-reference width is
`3050369419509665888336913191617402631153 / 41231686041600000000000000000000000000000000000000000`.

Exact-self error is zero and qualifies only the reference implementation. The
source refuses arbitrary caller-authored candidate PASS records: validation is
restricted to the freshly rebuilt exact-self candidate bound to the frozen
certificate/table/grid digests. A learned or nonzero candidate requires future
independently authenticated computation evidence.

## 8. Additive count effect and preserved nonclaims

The bound predecessor state is 158 open and eight closed PRE fields, six open
and zero closed POST fields, and 164 open/eight closed fields in total. This
package closes exactly F007--F018:

- PRE becomes 146 open and 20 closed;
- POST remains six open and zero closed; and
- total becomes 152 open and 20 closed.

F148 remains the earlier separate frozen infrastructure-rerun predicate
`NEVER_TRUE_NO_INFRASTRUCTURE_RERUN`.  The separately authorized V4
terminal-PASS custody registration is an event/artifact, not F148.  No
other field changes. B05 remains open because F114--F127, F149, execution, and
the remaining B05 evidence are absent. Gate A, Formal Tests 28 and 29 remain
OPEN, Formal Test 30 remains PENDING, all blockers,
R1, R2, C17, results, data/runtime/science, and submission remain open or
unperformed. This package does not edit any tracker.

The package is internal control evidence only. It is not anonymous/public
manuscript text and requires a fresh anonymity, methods, statistics, and claim
boundary audit before any external use.
