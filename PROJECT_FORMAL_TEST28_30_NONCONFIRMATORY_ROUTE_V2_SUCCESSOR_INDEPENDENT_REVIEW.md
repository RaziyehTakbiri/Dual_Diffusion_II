# Independent review — Formal Tests 28–30 nonconfirmatory route V2 successor

## Decision

**GO — authorize exactly the existing timetable registration**

`Required Tests 28–30 routes run end to end on nonconfirmatory/synthetic inputs.`

Final finding counts are **P0/P1/P2 = 0/0/0**.

The V2 successor closes the rejected V1 route's sole P1.  Its exact accepted
whole-method predecessor now carries the selected Test-28 initializer
configuration through a typed transform and derived initial state into actual
bounded Test-29 lineage/edit and Test-30 addressed-Heun path operations.  The
route independently revalidates that accepted whole-method machine and GO
review, preserves the historical CP63 boundary, freshly executes the complete
1,024-case Test-29/Test-30 supplied-input route, and binds all three components
under strict semantic schemas and a new V2 subject and receipt.

This GO authorizes no other registration.  Formal Tests 28 and 29 remain
`OPEN`; Formal Test 30 remains `PENDING`.  It does not close B12, a field,
blocker, result slot, runtime, data, training, scientific, production, claim,
release, or authority predicate.  No candidate, tracker, or ledger file was
edited during review.  This independent-review document is the review lane's
only created file.

## Exact reviewed package

| Role | Path | Bytes | Raw SHA-256 |
|---|---|---:|---|
| Human candidate | `PROJECT_FORMAL_TEST28_30_NONCONFIRMATORY_ROUTE_V2_SUCCESSOR.md` | 6,735 | `fd7a2a5db5e34f25e5be45d606aa5c4153e8a80059d71593588e7e4a0b3c04dd` |
| Route source | `src/heterodiff/evaluation/formal_test28_30_nonconfirmatory_route_v2.py` | 37,060 | `ffbe30e00629e2fab14148c5c8593604eaccf5c9b22d75cf8566ab21868f72fa` |
| Canonical machine record | `research/fixtures/manuscript_v3_formal_test28_30_nonconfirmatory_route_v2_successor.json` | 2,495 | `0a0c67721ca8cfa5e916389787f7b4672e9a38790e81ab19f842f687e02332cf` |
| Hash-first validator | `research/diagnostics/manuscript_v3_formal_test28_30_nonconfirmatory_route_v2_successor.py` | 10,557 | `1fdfff96645cdd7a975003dd28fe446233629008ef83319cc5648f61a3ef794a` |
| Focused hostile tests | `tests/unit/test_formal_test28_30_nonconfirmatory_route_v2.py` | 21,469 | `2568810b0c03a96ebf98b0422598ff11b8643886bbbe599cd8bc44826f49f7c3` |

All five frozen files were exact regular `0644`, single-link files before and
after execution.  The machine is duplicate-key-free canonical ASCII JSON with
a terminal LF.  Its independently recomputed self-record is
`39a0cb5b95bf81759f704128989404d424f019188876648ec5be28279a6e9378`.

The independently reproduced V2 route-subject digest is
`a1fb05402feb3efbf7a413e36e3eafc6528ec811ce9768d2bf49332e57d67a03`.
The 6,513-byte canonical route receipt is
`eab918f4aa9a58f56466673f5e8bcaefb6180692acbdfd9a6e6a694c2a3b6c4f`.

## Independent reproduction

The review used Python 3.11.5 and pytest 9.1.1.  Bytecode and pytest-cache
writes were disabled for the final test runs.  The standalone validator passed
from the project root:

```text
PASS_FORMAL_TEST28_30_ROUTE_V2_SUCCESSOR — record 39a0cb5b95bf81759f704128989404d424f019188876648ec5be28279a6e9378; receipt eab918f4aa9a58f56466673f5e8bcaefb6180692acbdfd9a6e6a694c2a3b6c4f
```

The focused suite separately exercised the validator from the project root and
from an unrelated physical copy; both passed.  Exact final test totals were:

- frozen V2 focused and hostile suite: **69/69 passed** in 99.29 seconds;
- exact bounded predecessor compatibility roster: **105/105 passed** in
  64.38 seconds; and
- total distinct focused plus compatibility checks: **174/174 passed**.

The 105-test compatibility roster was the accepted CP63 Test-28 independent
recomputation suite, the hash-first Test-29/Test-30 two-macrostep parent-custody
suite, and the predecessor whole-method nonconfirmatory runner suite.  A
separate two-case invocation of the root and unrelated-copy validator tests
also passed **2/2**.

## Three-component end-to-end finding

### Historical CP63 Test-28 route

The V2 source captures and compiles the exact rejected V1 route source only to
reuse its accepted CP63 and Test-29/Test-30 component implementations; it does
not call or accept V1's rejected whole-method binding.  It then applies its own
strict exact-schema validators before either component can enter the V2
subject.

The CP63 component binds the exact accepted fixture, runner, independent
recomputation, acceptance receipt, semantic pin, and recomputation digests.  It
requires exactly 16 rows, two repetitions per row, 32 launches, and 554
estimands, partitioned into 72 observable, 170 rejection-first-attempt, and 312
selected-feature estimands.  It truthfully records
`historical_nonconfirmatory_execution_receipt_revalidated=true`,
`fresh_execution_performed_here=false`, and `confirmatory_evidence=false`.

### Fresh bounded Test-29/Test-30 route

The exact hash-first wrapper and its four exact parent sources execute afresh.
The receipt requires 1,024 ordered word-pair cases, 1,024 distinct input
digests, 1,024 distinct report digests, parent-source custody, captured-byte
compilation, and report digest
`2a278585373d017b3b60bed28dcbc0ab3830f72c0512891658fc2ab54c666d53`.
It truthfully records `fresh_execution_performed_here=true` and
`confirmatory_evidence=false`.  This is bounded supplied-input route evidence,
not a general path or Formal-Test result.

### Accepted repaired whole-method bridge

The V2 route binds and revalidates the exact accepted whole-method successor:

- machine raw SHA-256
  `e247add0ef427cfd2c77f27a9347c28bc0b70df8bb53b803864619d27d1e7ea8`;
- machine self-record
  `384f4a15fa28c18f3e84177f7bc42332b612ac8ed3afff6a5a5ee918b02ab107`;
- stable receipt
  `7f3af61499f4c618daa38d72e38570c4759c5e146eeeef61bb182b9b4f20e102`;
- equal primary and independent core
  `73887c5411e8822942c9c37ddbdfb1a485f96ef1a2fce4c4ff56f503b4b9bc8e`;
  and
- independent GO review raw SHA-256
  `e24f2e97a67048323170b44d6e537ab07c3a7a6692cf682bc3053c1165732765`.

The route recomputes the whole machine self-record, canonical core, whole
receipt, supplied input, selected-state, transform, path-input, path-report,
and end-to-end custody digests.  It requires the complete accepted chain:

- supplied input:
  `f7e213442d073f88df73d2b33c21e43add4269a8a45b07714bfbd60b4b4ff971`;
- stable initializer execution:
  `5bca3f822a6a526fb0775cc7bf422347df7e65227141b4f2c76a462d3d597f85`;
- selected configuration:
  `c9450132be2800eddc7e8e36547c49e8b7839e1e282e32f0736b453267b92b06`;
- transform:
  `72a27a8f315e4a1fa95933fde4fe8711d08bcd1d00766dea80bf50275ebcb5b4`;
- derived initial state:
  `2338839b5c7df9c0845063a4053e6ab40d16132f232713ef515d5599d728f05f`;
- integrated path input:
  `63aa54613cad1b89973bbceb83cab2479f2cacbcd92e8c2b925f1ff34912f9a4`;
- integrated path report:
  `15b46792946d98a9893ba3b7fe31ff83c483500c45751564c8bfbbe9fb247b81`;
  and
- end-to-end custody:
  `037d50b89289979c8b40bc843f14fd47fc0365792c0b12c4315b4132c6e428ca`.

The stable initializer particle/ESS/stream projection is not present in the
machine core, so V2 correctly treats its digest as transitively bound accepted
evidence rather than claiming a fresh reconstruction.  The remaining chain is
recomputed from the captured machine.  Positive flags require Test-28 local
initializer admissibility, initializer-to-path integration, bounded
two-macrostep integration, Test-29 route and lineage semantics, Test-30 Heun
primitives, isolated predecessor validation, independent GO, and equal separate
recomputation bytes.  This is the nondecorative data-flow bridge missing from
V1.

## Integrity, custody, and hostile review

The V2 subject contains the complete CP63 mapping, complete Test-29/Test-30
mapping, exact ordered component identifiers, and complete whole-method
successor binding.  The outer receipt separately binds that subject and every
state, scope, nonclaim, and proposed-task field.  Independent schema validators
require exact key rosters, built-in leaf types, file pins, counts, digests, and
historical/fresh flags.  Fully re-signed omission, substitution, and extra-key
forgeries fail; digest-shaped strings alone cannot enter the accepted subject.

Review and focused tests verified rejection of:

- the old V1 whole-method receipt, a NO-GO review, false Test-28 admissibility,
  chain-consistent wrong values, missing or forged nested components, and all
  whole-chain digest or positive-flag substitutions;
- integer/boolean type aliases in both decoded machine values and public
  receipt fields, malformed/nonfinite/noncanonical or duplicate-key JSON, and
  duplicate literal dictionary keys in the validator source;
- noncanonical roots and relative paths, root/intermediate/leaf symlinks,
  hardlinks, wrong modes, content drift, and final machine identity drift; and
- preloaded private-module cache poison, an explicit `None` cache entry, a truly
  absent cache entry, concurrent route calls, concurrent validator calls, and
  incorrect cache restoration.

The route and validator use descriptor-relative no-follow walks, require stable
regular `0644` single-link files with exact byte counts and hashes, compile
captured sources under private module identities, serialize the fixed module
cache transitions, and restore prior cache state.  The unrelated physical copy
passed without relying on the original project path.

## Exact authorization and unchanged state

This review authorizes later tracker registration of exactly one existing task:

- **eligible:** `Required Tests 28–30 routes run end to end on
  nonconfirmatory/synthetic inputs.`

It does not authorize any Formal-Test closure.  Exact continuing states are:

- Formal Test 28: `OPEN`;
- Formal Test 29: `OPEN`;
- Formal Test 30: `PENDING`;
- whole-method open residual slots: **50**; and
- confirmatory evidence: **false**.

Exact review deltas are therefore:

- review evidence for the one route task: **GO**;
- author-applied timetable checkbox delta: **0**;
- tracker and evidence-ledger edits: **none**;
- Formal-Test, field, blocker, result-slot, and B12 deltas: **0**; and
- runtime, data, network, entropy, training, science, production, claim,
  release, and authority deltas: **0**.
