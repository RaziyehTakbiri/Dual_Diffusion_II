# UNRESOLVED INPUT ONLY -- THIS FILE IS NOT A PRODUCTION LOCK.
#
# Resolve for CPython 3.12 on linux/amd64 using PyPI plus the PyTorch CPU wheel
# index. The resolver must generate exact transitive pins and SHA-256 hashes.
# The heterodiff wheel is built from the checked-in content-addressed source
# manifest, hashed, and bound separately; install it with --no-deps after the
# lock.
# Never install this .in file directly on Databricks.

--extra-index-url https://download.pytorch.org/whl/cpu

numpy==2.4.6
scipy==1.17.1
threadpoolctl==3.6.0
torch==2.12.1+cpu

# Pinned development/verification tooling available to the route.
pytest==9.1.1
pyflakes==3.4.0
# DBR 17.3 ships mlflow-skinny 3.0.1, which requires packaging<26. Keep the
# project overlay compatible with that base-runtime constraint.
packaging==25.0

# Exact bootstrap/build tooling. The project wheel is built with
# `pip wheel --no-deps --no-build-isolation`, so the separate `build` package
# is intentionally unnecessary.
pip==25.0.1
setuptools==74.0.0
wheel==0.45.1
